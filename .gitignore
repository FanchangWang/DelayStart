## A streamlined .gitignore for modern .NET projects
## including temporary files, build results, and
## files generated by popular .NET tools. If you are
## developing with Visual Studio, the VS .gitignore
## https://github.com/github/gitignore/blob/main/VisualStudio.gitignore
## has more thorough IDE-specific entries.
##
## Get latest from https://github.com/github/gitignore/blob/main/Dotnet.gitignore

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# .NET Core
project.lock.json
project.fragment.lock.json
artifacts/

# ASP.NET Scaffolding
ScaffoldingReadMe.txt

# NuGet Packages
*.nupkg
# NuGet Symbol Packages
*.snupkg

# Others
~$*
*~
CodeCoverage/

# MSBuild Binary and Structured Log
*.binlog

# MSTest test Results
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*

# NUnit
*.VisualState.xml
TestResult.xml
nunit-*.xml

## ==================================================================
## DelayStart 项目专属
## ==================================================================

# 需求分析用的参考工程。仅用于提取 Win32 / 注册表 / 计划任务的调用方式，
# 其中的代码不是本项目的范例，不纳入仓库。
demo/
demo2/

# WorkBuddy 工作目录：项目记忆（.workbuddy/memory）与临时校验产物（.workbuddy/tmp）
.workbuddy/

# 原型 / 文档的本地校验产物
*.tmp.js

# DelayStart 运行时数据目录（正常情况下在 %LOCALAPPDATA%\DelayStart\，
# 此处兜底防止测试时误落在仓库内）
scheduler.log
manager.log
current-run.json
runs/
state/
logs/

# IDE / 编辑器本机状态。
# 上面那份 Dotnet.gitignore 只管 bin/obj，不管 IDE 目录，
# 而本项目用 VS 2026 开发，至少这些必须挡住：
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates
*.csproj.user

# 本地探测 / 临时校验产物（AI 与环境排查用，不入库）
*.tmp.txt

# 打包产物兜底。当前分发走 unpackaged + Inno Setup（D22），不产 MSIX，
# 这几条是为防将来误用 dotnet publish / MSIX 工具链时把产物提交进来。
# 原先这些规则写在 src/DelayStart.App/.gitignore（模板生成），已合并到此处。
AppPackages/
BundleArtifacts/
*.msix
*.msixupload
*.appx
*.appxbundle
*.appxupload

# 本地 NuGet 源覆盖（若某台机器需要走代理或镜像，用 NuGet.config 的本地覆盖，
# 不要把机器相关配置提交上去）
NuGet.config.local
