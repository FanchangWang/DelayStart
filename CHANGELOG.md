# 更新日志

本项目所有显著变更都记录在本文件。

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；版本号遵循语义化版本。
GitHub Release 的正文（功能变更 + 附件说明表）由 CI 依据本文件按标签提取生成 ——
**发版前必须先把对应 `## vX.Y.Z` 节写好**，否则 release job 会失败。

## 未发布

### 修复
- **「定义一致就跳过重写」在真机上不生效**：任务计划程序落盘时会把 Principal 账户名规范化成 SID，库读回 `Principal.UserId` 却是丢了域前缀的裸名（写入 `MSI\guyue`、读回 `guyue`），逐字段字符串比对永远不等 → 守卫 / 调度任务每次启动仍被重写、触发器被重置。现账户字段改经 SID 归一化比较，三种形态（全名 / 裸名 / SID）任意组合都能判同一账户（D114 真机首验修正）。
- **打开管理端会让守卫本次会话的巡检被静默吞掉**：守卫的触发器是「登录后 N 分钟执行一次」，登录事件只能消费一次。管理端启动期每次都重写 `\DelayStartGuard` 任务（`CreateOrUpdate`），等于用一个全新 LogonTrigger 替换定义；而此时登录事件早已过去，新触发器在本会话内永远等不到下一次登录 → 那次巡检被静默丢弃，任务计划程序里「下次运行时间」还卡在昨天。改为**先逐字段比对现有定义**（action 路径 / 触发器延迟·重复 / 身份 / 六项 Settings / 描述），完全一致就跳过写入、保留已武装的触发器；只有档位调整、安装目录迁移或任务被改坏时才真正重写（D112）。
- **调度计划任务也改为"定义一致就跳过重写"**：`\DelayStartScheduler` 此前每次启动都无条件重写（`CreateOrUpdate`），同样会重置登录触发器、覆盖「上次运行时间」等统计。现镜像守卫的 F1 做法——`TaskRegistrationService.RegisterOrUpdate` 写入前先逐字段比对现有定义（action 路径 / 工作目录、Principal 三项、触发器延迟·用户、六项 Settings、描述），完全一致即跳过写入、保留已武装的触发器与运行统计；缺失或定义确实变了（安装目录迁移 / 任务被改坏）才重写（D113）。

### 重构
- **守卫与调度计划任务的注册机制合并为一份**：两套逐字拷贝的注册 / 比对 / 删除实现（含各约 70 行的定义比对）收敛为唯一一份 `ScheduledTaskGateway`，期望定义用纯数据 `ScheduledTaskSpec` 描述；守卫与调度注册器瘦身为策略适配器，两个接口、全部调用方与测试**零改动**。调度侧两条异常消息随之更完整（补"调度"主语，D114）。

### 新增
- **守卫巡检结构化归档**：每次巡检完成后在 `%LOCALAPPDATA%\DelayStart\guard\inspections\` 落一份 `<时间>.json`（保留 30 份），完整记录纠正明细（条目名 + 成败）、新增条目（名字 + 来源）与失效条目 —— 此前这些明细只在系统通知里出现过（超过 3 个还折成"等 N 项"），巡检一结束就无处可查。写入失败只记日志、不影响巡检本身（D116）。
- **守卫日志页**：左侧导航「调度日志」下方新增一项，数据源为守卫巡检归档（`guard\inspections\*.json`），版式与调度日志页同构 —— 一次巡检一组，组标题 = 时间 + 汇总（与 guard.log「巡检完成」行、总览卡同一口径），组内明细表（列头 = 类别 / 名称 / 说明）能看到纠正了什么（失败标红）、新增了什么（名字 + 来源）、失效了什么（名字 + 原因）与来源失败（标红）；纠正 / 新增 / 失效全为 0 的组展开后只有一行描述文字，不摆空表格；最新一组默认展开；筛选为「全部巡检 / 仅含变化与异常」。guard.log 文本日志保留双轨，「打开日志文件」按钮不变（D115–D117）。
- **旧数据目录一次性迁移**：升级后首次打开管理端（图形界面或 CLI 命令）时，自动把旧的 `state\current-run.json` 与 `runs\*.json` 按新结构搬进 `scheduler\`（文件名不变）；迁移幂等、失败只记日志不阻塞启动，旧目录搬空即删除。运行几个版本后该迁移代码将整体移除（D116）。
- **「关于」页开源致谢**：新增「引用的开源库」清单（Windows App SDK、CommunityToolkit.Mvvm、CommunityToolkit.WinUI、Microsoft.Extensions.DependencyInjection、TaskScheduler、xUnit v3，各带用途与许可证、直达项目主页）与节假日数据来源（NateScarlet/holiday-cn）的说明与感谢（D115）。
- **README 开源致谢节**：列出全部引用的开源库与节假日数据来源，并向各项目作者与贡献者致谢（D115）。

### 变更
- **运行时数据目录按进程归堆（D116）**：调度端的实时状态与运行归档收进 `%LOCALAPPDATA%\DelayStart\scheduler\`（`current-run.json` 与 `archive\<runId>.json`，原 `state\`、`runs\` 目录撤销），守卫的数据集中在 `guard\`；目录名即进程名，与 `logs\` 下的文件名对得上。旧位置的数据由迁移逻辑自动搬入新结构，调度日志页的历史记录不受影响。
- **巡检汇总文案统一口径**：guard.log「巡检完成」行、守卫日志页组标题、总览「上次守卫巡检」卡三处共用同一份生成逻辑（`GuardRunSummaryText`），任何一处改措辞都只改一处。
- **「运行日志」更名为「调度日志」**：左侧导航菜单、页面标题、总览卡片入口、调度端托盘菜单与进度面板按钮、通知点击落点文案等全部同步；日志文件名与导航标签不变，历史归档不受影响（D115）。
- **总览页分节合并（D117）**：「开机调度任务」与「自启动项守卫」合并为「后台任务」分节 —— 两张卡片各左两行文字（标题 + 一行说明），右侧 = 状态 / 按钮 / 下拉（调度卡右侧为「✓ 已启用」状态，失败时换「✕ 未就绪」+「重试创建」）；「上次开机调度」与「上次守卫巡检」合并为「最近记录」分节 —— 左 = 汇总 + 时间（含失败项标红），右 = 直达对应日志页，无记录时左侧换成两行提示、右侧链接保留。删除档位说明行与任务注册细节等无用提示。

## v0.3.2 - 2026-09-24

### 新增
- **「关于」页**：左侧导航**底部**新增一项，版式与总览一致（限宽 1000 居中）—— 应用版本与定位、运行环境（.NET 运行时 / 操作系统 / 运行身份 / 架构）、四项数据位置（配置 / 数据 / 日志 / 安装目录，各带「打开」按钮）、开源与联网说明。另有一枚「**复制诊断信息**」，一键把版本、系统与四项路径复制成纯文本，提交问题时可直接附上。

### 修复
- **偶尔往窗口里拖文件完全没反应**：拖放接收器判断"这个窗口是否已启用过"时用的是 **HWND 值**，而 HWND 会被系统回收复用给新窗口 —— 复用命中就被跳过子类化，而 `DragAcceptFiles` 在此之前已经执行过（资源管理器照发 `WM_DROPFILES`），消息落进默认过程，不报错也没有日志。改为判断"窗口当前的过程是不是我们的"。
- **每次「刷新本页」都会在扫描缓存里留下陈旧条目**：局部重扫时条目列表与失败列表都按来源剔除了旧值，只有图标字典是**整份复制** —— 旧来源的条目已不在快照里，却仍是字典的键，而下一轮重扫又从这里复制一遍。现在只保留留下来的那些条目的图标。

### 变更
- **应用发布者改为 `FanchangWang`**：安装器写进「设置 → 应用」的发布者此前是裸 `DelayStart`，现与仓库账号、winget 包标识前缀（`FanchangWang.DelayStart`）统一。**老用户升级后该处显示名会变**（纯显示，无功能影响）。
- **Release 附件说明补上架构**：x64 自包含版此前只写「**大多数人的选择** · 自包含运行时，装完即用」，读者得从文件名里自己找架构。

## v0.3.1 - 2026-09-24

v0.3.0 之后的补丁：设置页两节的版式与交互重做、列表对齐修到同一条基线、节假日自动更新的静默问题修掉。

### 变更
- **设置页的「延时」与「周期」两节改为可折叠列表**：节头整行可点、右端一枚**纯图标**三角（不再是看起来像按钮的 `ToggleButton`），默认收起；展开后子内容左右内收，第一条固定是「新建」行，第二条起才是数据行、每条各占一张边框。
- **设置页延时行与周期行重排**：延时行左「登录后 n 秒 / n 分 m 秒（xxx 秒）」、右「设为默认」「删除」，当前默认那行显示绿色「当前默认」。周期行把内置 5 档一并列出（编辑 / 删除置灰，右列写明「内置周期，禁止删除」），「包含哪些天」由「每天」改为**逐天列出**（周一…周日）。
- **0 秒延时的文案统一为「登录后立即」**：设置页预设延时行（此前「登录后 0 秒」）与延时启动页的分组标题（此前「立即启动」）都用这个写法；列表延时列仍是裸的「立即」—— 那一列按列宽排版，整列本来就无前缀。
- **延时弹窗里的调度周期不再区分内置 / 自定义**，按顺序排在同一个面板里。
- **列表列头一律左对齐**，并与行内文字共用同一条左基线（总览 / 延时启动 / 自启动项 / 服务与驱动 / 运行日志）。
- **运行日志的条目状态文案改为纯中文**（成功 / 失败 / 跳过 / 启动），去掉 `✓` `✗` `◌` 前缀 —— 那些符号不在同一套字宽度量里，逐字符试探宽度没有终点；状态差别改由**颜色**表达。

### 修复
- **设置页「展开 / 收起」那行，鼠标移上去只有文字那一小块变色**：卡面原先画在外层 `Border` 上，按钮的悬停高亮被 `Padding` 关在内边距以内（四周各让出 16 / 14，圆角还比卡片小一圈）。改为**按钮自己画卡面**（`BasedOn="{StaticResource DefaultButtonStyle}"`，卡面 / 描边 / 圆角 / 内边距与其它卡片逐项一致），悬停与按下时整张卡片连描边一起响应。
- **列头与行内容的左基线不一致**：① 自启动项 / 服务·驱动 / 运行日志的**列头**把不属于自己的容器内缩也算进去了（列头比行多缩 8）；② 总览「最近一次开机调度」的小表**从未挂 `ItemContainerStyle`**，落到 WinUI 默认 `ListViewItem`（`Padding 12,0`、`MinHeight 40`），行比列头多缩 12、行高还被撑到 40。现在左右内缩只由「卡片 Border + 行 Grid」两处决定，行容器一律 `Padding=0`，列头 `Padding` 直接等于行 `Grid` 的 `Padding`。
- **运行日志页的分组容器一并对齐**：外层分组列表此前也没挂容器样式，整组（组标题 + 表头 + 行）比卡片边框多缩 12，现同样归零。
- **「自动更新节假日数据」再次静默**（某年数据文件被改名后不再重试）：节流把「已获取」与「上游未公布」合并成一个成功态，于是"记录说数据已写好、而它现在不在"这件事**无法表达**，7 天安静期退化成永久静默 —— 用户看到"开关是开的却没反应"，年份行还说"未下载"。结局拆成三档（已获取 7 天 / 上游未公布 7 天 / 失败 1 小时），且**本地无可用数据时不再受"已获取"节流阻挡**；年份行与副标题同时给出"报告已获取、本地读不到"的说明。

## v0.3.0 - 2026-09-23

### 新增
- **调度周期（FR-15）**：每个延时条目挑一个**周期**决定这次登录要不要启它 —— 内置「每天 / 周一至周五 / 周六日 / 法定工作日 / 法定节假日」5 档，外加可命名的自定义周期（如「上一休一」「周末班」）。条目**引用**周期而非拷贝快照：改一处，所有引用它的条目同步生效；被引用的周期不允许删除。
- **法定日历离线判定**：管理端从 `holiday-cn`（国务院放假安排的社区镜像）拉取并归一化成 `%LOCALAPPDATA%\DelayStart\holidays\{year}.json`；调度端与守卫端**只读本地文件、零网络**（NFR-x）。调休补班日按数据判定 —— 例如 2026-09-20（周日·中秋前补班）在「法定工作日」档下照常启动。
- **设置页新增「周期」与「节假日数据」两节**：周期的新建 / 重命名 / 改星期 / 删除（带引用数提示）；节假日数据的**自动检查**、重新下载、补齐缺失年份、**从文件导入**（直接支持上游原始格式）、导出、打开数据目录。
- **运行日志记录"今天不启动"**：不在周期内的条目照旧不启动，但在运行日志里留一行「跳过」+ 原因 —— 不再是一个安静的早晨、界面上却什么都查不到。
- **延时启动列表**：程序名旁新增周期徽标（内置「每天」也显示，不用猜"没显示是不是没设"）与「今天不启动」徽章，悬停给出周期与判定原因。

### 变更
- 运行日志的条目状态文案统一为两个汉字宽：成功 / 失败 / 跳过。
- 本轮之前"不匹配周期 = 完全不可见"的口径改为**不启动但留痕**。

### 修复
- **设置 → 节假日数据 → 导出点击即闪退**：`FileSaveDialog` 被强转成**平行**接口 `IFileOpenDialog`，QueryInterface 返回 `E_NOINTERFACE`，抛出的 `InvalidCastException` 在事件处理器里无人接 → 整个进程静默消失。改为强转到两个 coclass 唯一的共同基接口 `IFileDialog`。
- **「自动检查」开着却不下载、设置页也没有任何提示**（两个真 bug 叠加）：① 三地址降级链把**自身的请求超时**当成终局 `return` —— 而在国内首个地址稳定 15 秒超时、两个 jsdelivr 镜像 1.1 / 1.5 秒就能返回 200，等于降级链在最常见的失败形态上完全不生效；② 节流在**发请求之前**就落时间戳，一次网络抖动静默 7 天。现在超时与 HTTP 错误、空内容一视同仁地"换下一个地址"，节流按**上次尝试的结局**分开（拿到数据 / 上游未公布 = 7 天，失败 = 1 小时）。
- **周期重名未拦截**：新增 `CycleNames`（Core）作为唯一判据，界面即时红字 + 落盘前兜底双保险；与内置五档同名同样拒绝。
- **延时弹窗里的"假交互"**：「包含哪些天」原先摆的是 `WeekdayGrid` 的**可编辑**形态（看起来能点、点下去什么都不变），改为一行纯文字。
- 设置页周期节两处常驻说明文字、法定两档冗长的提示句一并精简。

## v0.2.0 - 2026-09-23

### 新增
- **自启动项守卫**：常驻监测四类自启动来源的变化并发送系统通知，失效条目自动检测；纠正与记录不受影响。
- **管理端入口自提权**（D82）：应用清单改为 `asInvoker`，提权由程序入口按需申请，第三方启动路径（终端、`Win+R`）不再直接 740 报错。
- **调度完成系统通知**：新增通知中转器 `NotifyBroker`，收尾按策略发系统通知，点击通知直达运行日志页（N1–N12）；通知与完成面板彻底分离，面板改为手动弹出 + 「钉」住交互。
- **统一应用图标**：管理端、调度端、守卫与两个中转器全部使用新图标（含告警角标变体）。

### 变更
- **卸载流程重塑**（D84/D85）：桌面快捷方式恢复默认勾选（静默安装同样创建）；卸载时弹**原生任务对话框**选择配置与日志的去留（默认「保留配置并卸载」，点「取消」= 终止卸载且不碰任何接管项）；静默卸载支持 `/DELETEDATA` 参数；还原失败一律中止卸载，不再出现「点否也继续删」。
- README 按开源项目惯例重写，新增界面速览（截图）。

## v0.1.0 - 2026-09-22

首个公开版本。

### 新增
- **延时启动管理**：覆盖四类自启动来源 —— 注册表 Run 键、启动文件夹、计划任务、UWP 应用；支持接管 / 编辑延时 / 移出还原，Win32 服务与系统启动项只读展示。
- **调度端**：登录后按计划延时启动条目，逐条降权启动；托盘常驻，完成后弹面板倒计时；调度收尾按策略判定成功 / 失败 / 跳过。
- **管理端界面**：总览 / 延时启动 / 运行日志 / 设置四个页面，深浅主题，单实例互斥与唤起链诊断。
- **安装器**：Inno Setup 中文向导；full（自包含运行时）/ slim（框架依赖）双形态 × x64 / ARM64 四个安装包；卸载前先还原所有接管项。
- **CI**：推 `v*` 标签自动构建四安装包并发布 GitHub Release。
