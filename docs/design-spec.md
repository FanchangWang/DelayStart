# DelayStart — 界面设计与文案

> 状态：**待评审**。评审通过后才进入编码。
> 配套产物：`docs/api-analysis.md`（Demo 分析结论）、`docs/ui-mockup.html`（可交互原型）

---

## 一、产品定位

| 项 | 内容 |
|---|---|
| 中文名 | **延时启动管理器** |
| 英文名 | **DelayStart** |
| 一句话 | 把开机时一股脑抢占资源的自启程序，改成按你的节奏、错峰、延时启动。 |
| 副标语 | 不删除任何原始配置 · 全部可一键还原 |
| 目标用户 | Win11 用户，开机被 20+ 个自启程序拖慢，但又不舍得直接禁用某个程序 |
| 核心价值 | ① 全位置扫描（注册表 3 处 + 2 个启动文件夹 + 计划任务 + UWP）<br>② 软禁用，随时可还原<br>③ 延时可编排，处理程序间的启动依赖 |
| 与"直接禁用自启"的区别 | 禁用 = 永远不启动；延时启动 = **照常启动，只是晚一点**。适合"我需要它，但不想它开机就跟我抢资源" |

**明确的非目标（不做）**：
- ❌ 不做启动项清理/优化评分（那是另一个产品）
- ❌ 不做系统服务延时接管（服务用系统原生的「自动（延迟启动）」）
- ❌ 不碰内核驱动启动顺序
- ❌ 不修改 Winlogon / Userinit / Shell（只读展示）

---

## 二、信息架构

左侧 `NavigationView`，共 6 个顶层模块 + 「自启动项」下的 5 个子视图。

```
┌ 延时启动管理器 ────────────────────────────────────┐
│                                        [☀/🌙] [—]  │
├────────────────┬───────────────────────────────────┤
│ ⌂  总览         │                                   │
│ ▤  自启动项  13 │                                   │
│    ├ 全部       │          内容区                    │
│    ├ 注册表   6 │                                   │
│    ├ 启动文件夹 2│                                   │
│    ├ 计划任务  3│                                   │
│    └ UWP 应用  2│                                   │
│ ⏱  延时启动   4 │                                   │
│ 🛡  系统启动项   │                                   │
│ ▤  运行日志     │                                   │
│ ⚙  设置         │                                   │
├────────────────┴───────────────────────────────────┤
│ 调度任务：已创建 · 登录后 3 秒触发        [详情]     │
└────────────────────────────────────────────────────┘
```

导航栏为什么把「来源」做成子项：这个软件的差异化就是「扫得全」。把 5 个来源平铺在导航上，用户一眼就知道程序查了哪些地方、各自查到几个 —— 比藏在筛选器里强。

### 搜索框为什么从导航栏挪进页面

**原始设计是错的。** 导航栏顶部放一个「搜索启动项」输入框，效果是：用户在「延时启动」页打字，页面被强制跳转到「自启动项」页 —— 用户的上下文被抢走。

改成**页面内搜索**，两处各自独立、各搜各的：

| 位置 | 搜什么 | 匹配字段 |
|---|---|---|
| 自启动项页工具栏 | 系统里已有的自启动项 | 名称 / 路径 / 命令行参数 / 值名或文件名 / 所在位置 / 来源类型 |
| 延时启动页工具栏 | 已接管的条目（含手动添加） | 同上 |

规则：
- 只过滤**当前页**的列表，绝不跳页、绝不改 `state.src`
- 搜索状态与页内筛选器（状态 / 排序）叠加生效，互不干扰
- 输入框有内容时右侧显示清空按钮；清空后恢复完整列表
- 「延时启动」页处于搜索状态时，「上移 / 下移」按钮禁用（顺序是全局语义，在过滤视图中调整会产生歧义），并在表下给出说明
- 空结果时给「清空搜索」按钮，不是干巴巴一句"没有数据"

导航栏顶部不再有任何输入控件。

---

## 三、逐页 UI 文案

### 页面 1 · 总览

**标题**：总览
**副标题**：开机自启动一览，以及延时启动的执行计划

**统计卡（4 张）**

| 卡片标题 | 数值示例 | 辅助文案 |
|---|---|---|
| 已扫描自启动项 | 15 | 其中 2 项已禁用 · 4 项为系统组件 |
| 已接管延时启动 | 5 | 系统自启动 4 项 · 手动添加 1 项 |
| 开机加载窗口 | 1 分 00 秒 | 从登录到最后一个程序启动 |
| 开机调度任务 | 已就绪 | 登录后 3 秒触发 · 以最高权限运行 |

> **计数口径**：`已扫描自启动项` 只数系统里扫到的条目（不含手动添加）；`已接管延时启动` 数所有被接管的条目，并拆出手动添加的数量。两个数字口径不同是刻意的 —— 第一个回答"系统里有什么"，第二个回答"我接管了什么"。

**「开机启动时间轴」卡片**
小标题：开机启动时间轴
说明：登录后各程序的启动时点
（横向时间轴，横轴标尺：`登录` → `10s` → `30s` → `60s` → `1min`，节点为程序图标+名称）

**「调度任务」卡片**
- 已就绪状态：`调度任务已创建` / `触发器：登录时，延迟 3 秒` / `运行身份：最高权限（不弹 UAC）`
- 缺失状态（警示色）：
  > ⚠ **尚未创建开机调度任务**
  > 延时启动不会生效。需要创建计划任务，在登录后唤起调度器。
  > [立即创建] [稍后再说]

**「最近调度」卡片**
小标题：最近一次开机调度
- 记录行：`✅ 微信 · 30s · 成功` / `❌ pot 翻译工具 · 60s · 启动失败（文件不存在）`
- 无记录时：`暂无调度记录，重启后在此查看`

**操作区**：`[刷新扫描]` `[立即模拟调度]`

> **「立即模拟调度」是刻意放在首屏的**。延时配置写完必须重启才能验证，体验很差。这个按钮让用户当场看到效果（0.5 倍速播放时间轴 + 真的启动程序），是开发调试和用户验收的双赢。

---

### 页面 2 · 自启动项

**标题**：自启动项
**副标题**：共 13 项 · 已接管 3 项 · 已禁用 1 项

**工具栏**（从左到右）
`[🔍 搜索名称 / 路径 / 值名]` `[来源 ▾]` `[状态 ▾]` `排序 ▾` … `[刷新扫描]`
批量模式下额外出现：`已选 3 项` `[批量延时启动]` `[批量禁用]` `[取消选择]`

> **「手动添加」按钮不在这里** —— 自启动项页的职责是「列出系统里已有的东西」，不是创造新东西。手动添加只存在于「延时启动」页。
>
> 由此引出一条数据边界：**手动添加的条目不进入本页**。它没有注册表值可以软禁用，也没有来源可以恢复，混进本页会让「禁用」这个动作变得无意义。本页的提示条里写明了这一点。

**筛选器文案**
- 来源：`全部来源` / `注册表` / `启动文件夹` / `计划任务` / `UWP 应用`
- 状态：`全部状态` / `已启用` / `已禁用` / `已接管` / `已失效`
- 排序：`按来源` / `按名称` / `按状态`

**列表行（示例）**

```
┌───────────────────────────────────────────────────────────────────────┐
│ [W图标] 微信                                      ● 已接管    [⋯]    │
│         C:\Program Files\Tencent\Weixin\Weixin.exe  -autorun          │
│         注册表 · HKEY_CURRENT_USER\...\Run · 值名 Weixin               │
│         延时 30 秒 · 普通用户                [移出延时]               │
├───────────────────────────────────────────────────────────────────────┤
│ [☁图标] OneDrive                                  ● 已启用    [⋯]    │
│         C:\Program Files\Microsoft OneDrive\OneDrive.exe  /background │
│         启动文件夹 · 用户启动文件夹 · OneDrive.lnk                      │
│                                     [延时启动]  [禁用]                │
├───────────────────────────────────────────────────────────────────────┤
│ [⚙图标] Adobe Acrobat Update Task                ○ 已禁用    [⋯]    │
│         计划任务 · \Adobe Acrobat Update Task · 登录时触发             │
│         启动身份 普通用户                                     [启用]  │
└───────────────────────────────────────────────────────────────────────┘
```

**状态徽标文案与配色**

| 徽标 | 含义 | 配色 |
|---|---|---|
| `已启用` | 系统会正常自启 | 中性灰描边 |
| `已禁用` | 已通过软禁用标记阻止自启 | 警示黄 |
| `已接管` | 原自启已禁用，改由本程序延时启动 | 强调色实心 |
| `已失效` | 路径不存在 / 任务已被删除 | 危险红 |
| `受保护` | 系统组件，不可修改 | 灰色 + 锁图标 |

**内嵌说明文案**（列表顶部常驻一条轻量提示条）
> 这里的「禁用」与「移出延时」都是**软禁用**：不会删除注册表值，也不会移动文件，随时可以还原。<br>
> 本页只列出**系统里已经存在**的自启动项。手动添加的程序属于调度任务而非系统自启动项，只出现在「延时启动」页。

**行内操作菜单 `⋯`**
- `打开文件位置`
- `复制命令行`
- `查看来源详情`（弹出：完整注册表路径 / 任务 XML / AUMID / 快捷方式目标）
- `在任务管理器中查看`
- →（已接管项）`编辑延时设置`

**空状态**
- 筛选无结果：`没有匹配的启动项` / `试试更换筛选条件或清空搜索关键词`
- 该来源本就为空：`此位置没有自启动项` / `注册表 ...\Run 下没有找到任何条目`

---

## 三之二、延时配置编辑器（弹窗）

「自启动项」页的 `[延时启动]`、「延时启动」页的 `[手动添加]` 和 `[编辑]`，**共用同一个弹窗组件**，靠"是否已绑定系统来源"分叉。这样用户只需要理解一套交互。

### 4 种进入方式 → 3 种形态

| 入口 | 目标程序 | 可编辑字段 | 底部主按钮 | 第 4 块内容 |
|---|---|---|---|---|
| 自启动项页 `[延时启动]` | **只读**（由系统项绑定） | 参数 / 延时 / 身份 | 加入延时启动 | 接管对象 |
| 延时启动页 `[编辑]`（系统来源） | **只读** | 参数 / 延时 / 身份 | 保存修改 | 接管对象 |
| 延时启动页 `[编辑]`（手动条目） | 可更换 | 名称 / 参数 / 工作目录 / 延时 / 身份 | 保存修改 | 系统影响 |
| 延时启动页 `[手动添加]` | 可选择 | 名称 / 参数 / 工作目录 / 延时 / 身份 | 加入延时启动 | 系统影响 |

分叉的唯一依据是**这条记录有没有系统来源**。有来源 → 路径由系统项决定，不允许改；没来源 → 路径是这个记录的全部意义，必须能改。

### 布局

```
┌──────────────────────────────────────────────────────┐
│  手动添加延时启动                                [×]  │
│  选择一个程序，由本程序在登录后延时启动                │
│                                                      │
│  ① 目标程序            拖入文件或点击选择             │
│  ┌────────────────────────────────────────────────┐  │
│  │              [文件图标]                        │  │
│  │      点击选择程序，或把文件拖到这里              │  │
│  │  支持 .exe / .lnk / .bat / .cmd / .msi         │  │
│  └────────────────────────────────────────────────┘  │
│    ── 选择后替换为 ──                                │
│  ┌────────────────────────────────────────────────┐  │
│  │ [手] Ditto 剪贴板                    [更换]     │  │
│  │      D:\Tools\Ditto\Ditto.exe                  │  │
│  └────────────────────────────────────────────────┘  │
│  显示名称 — 自动取自文件名，可改                      │
│  [ Ditto 剪贴板                              ]       │
│  命令行参数 — 可选                                    │
│  [                                           ]       │
│  工作目录 — 可选，留空则用程序所在目录                 │
│  [ D:\Tools\Ditto                            ]       │
│                                                      │
│  ② 延时时间            相对登录时刻的时间点            │
│  ( )登录后立即 ( )10 秒  (•)30 秒                     │
│  ( )60 秒     ( )2 分钟                              │
│  自定义 [ 30 ] 秒                  预设值可在「设置」里改│
│  延时是相对登录时刻的绝对时间点，不是累加。同一时间点   │
│  的多个程序会几乎同时启动。                           │
│                                                      │
│  ③ 启动身份                                          │
│  (•)普通用户（推荐）                                  │
│     以当前登录用户身份启动，拖拽、剪贴板、文件权限都正常 │
│  ( )管理员                                           │
│     由调度器在最高权限下启动，不会弹 UAC 确认框        │
│                                                      │
│  ④ 系统影响                                          │
│  ┌────────────────────────────────────────────────┐  │
│  │ 条目类型   手动添加 · 不属于系统自启动项          │  │
│  │ 写入位置   %LOCALAPPDATA%\DelayStart\config.json│  │
│  │ 系统影响   不改动注册表、启动文件夹、计划任务；    │  │
│  │            移除时直接删除本条配置，无残留          │  │
│  └────────────────────────────────────────────────┘  │
│                                                      │
│  将在 登录后 30 秒 以 普通用户 身份启动   [取消][加入] │
└──────────────────────────────────────────────────────┘
```

### 设计要点

- **步骤编号 ①②③④** 把长表单切成 4 块。原来的弹窗是一堆无标题的控件堆叠，看不出结构。
- **目标程序是唯一的大控件**。它是这一步的全部信息量，用整块虚线拖放区承接，而不是一个普通输入框 + 一个浏览按钮。选中后原地变成「图标 + 名称 + 路径 + [更换]」，视觉上"填好了"这件事一目了然。
- **第 4 块是"会动到系统什么"的交代**。有系统来源时列出资源位置和接管动作（强调软禁用）；手动条目时反过来强调**不动系统任何东西**。两种情况下用户最关心的都是同一件事：我这次操作会不会搞坏系统。所以这块不能省。
- **底部摘要是活的**：`将在 登录后 30 秒 以 普通用户 身份启动`，随延时/身份选择实时更新。用户不用回去逐项核对就能确认最终结果。
- 弹窗宽度 620px（比确认类弹窗宽），容纳拖放区。

### 各块文案

| 块 | 标题 | 副标题 |
|---|---|---|
| ①（可选文件） | 目标程序 | 拖入文件或点击选择 |
| ①（只读） | 目标程序 | 由系统自启动项绑定，不可更改 |
| ② | 延时时间 | 相对登录时刻的时间点 |
| ③ | 启动身份 | — |
| ④（系统来源） | 接管对象 | — |
| ④（手动条目） | 系统影响 | — |

**弹窗标题 / 副标题**

| 场景 | 标题 | 副标题 |
|---|---|---|
| 手动添加 | 手动添加延时启动 | 选择一个程序，由本程序在登录后延时启动 |
| 加入系统项 | 加入延时启动 | 来自「注册表」，加入后原自启动项将被软禁用 |
| 编辑系统项 | 编辑延时设置 | 修改已接管条目，原自启动项保持软禁用 |
| 编辑手动项 | 编辑延时设置 | 手动添加的条目，不关联任何系统自启动项 |

### 字段占位符

| 字段 | placeholder |
|---|---|
| 显示名称 | 例如：微信 |
| 命令行参数 | 留空则不附加参数 |
| 工作目录 | 留空则使用程序所在目录（未选程序时为「选择程序后自动填入」） |

### 拖放区文案

- 空态主文案：`点击选择程序，或把文件拖到这里`
- 空态副文案：`支持 .exe / .lnk / .bat / .cmd / .msi`
- 文件类型白名单是硬性的：`.exe` `.lnk` `.bat` `.cmd` `.msi`。不接受 `.dll`、文件夹、文档。

### 「移除延时」确认弹窗（分两种文案）

| 条目类型 | 正文 | 主按钮 |
|---|---|---|
| 系统来源 | 该程序的原始自启动项将恢复为**你接管前的状态**。<br>下次登录时它会按系统原本的方式启动，不再受本程序控制。 | 移除并恢复 |
| 手动添加 | 该条目是**手动添加**的，不关联任何系统自启动项。<br>移除后只删除本程序里的这条配置，**系统的任何设置都不会被改动**。 | 移除条目 |

> 手动条目的按钮措辞不能叫「移除并恢复」—— 没有东西可恢复，这个说法会让人以为动了系统。

---

### 页面 3 · 延时启动

**标题**：延时启动
**副标题**：共 5 项（含手动添加 1 项）· 最后一个在登录后 1 分 00 秒启动

**工具栏**：`[🔍 搜索已接管的程序]` `[列表 | 时间轴]` … `[立即模拟调度]` `[导出]` `[导入]` `[＋ 手动添加]`

**列表视图**

| 列 | 内容 |
|---|---|
| 顺序 | `1` `2` `3`，同延时内可上下调序 |
| 名称 | 带图标 |
| 命令 | 路径 + 参数 |
| 延时 | `登录后 30 秒`（可点击内联修改） |
| 身份 | `普通用户` / `管理员` |
| 来源 | `注册表 HKCU` / `用户启动文件夹` / `计划任务` / `UWP` / `手动添加` |
| 操作 | `↑` `↓` `编辑` `移除` |

搜索状态下的行为：
- 只影响列表内容，不影响页头统计（统计反映全局）
- `↑` `↓` 置灰，表下补一句：`搜索状态下暂时无法调整顺序。`
- 时间轴视图同步过滤；无匹配时显示 `没有匹配的条目。清空搜索框可恢复完整时间轴。`

**常驻提示条**
> 延时时间是**相对登录时刻**的时间点，不是累加。同一时间点的程序按列表顺序依次发起启动。
> 如果 B 依赖 A 先启动，请给 B 设置**更大**的延时值。

**时间轴视图**
横轴：`登录` → `10s` → `30s` → `60s` → `1min` → `2min`
节点按延时值定位，同一时点的多个条目纵向堆叠并标注发起顺序。
末端标注：`开机加载窗口 1 分 00 秒`

**空状态（两种，要分清）**

一条都没有时：
> **还没有延时启动项**
> 到「自启动项」页找到想延后的程序，点击「延时启动」；或者直接手动添加一个程序。
> [前往自启动项] [手动添加]

有数据但搜索无结果时：
> **没有匹配的延时条目**
> 换个关键词试试，或清空搜索框。
> [清空搜索]

**「移除延时」确认弹窗** → 见三之二节末，按条目类型分两种文案。

---

### 页面 4 · 系统启动项（只读）

**标题**：系统启动项
**副标题**：服务、驱动、登录组件的只读检查

**顶部常驻警示条**
> 🛡 **此页仅供查看，不参与延时启动**
> 系统服务、内核驱动、Winlogon 组件的启动顺序由 Windows 控制。强行延时会拖慢或破坏系统启动，这里只做展示与安全排查。

**分组与文案**

**① 系统服务（自动启动）**
> 服务的正确延时方式是改用 Windows 原生的「自动（延迟启动）」——系统启动约 2 分钟后拉起，比本程序接管更安全。
> 列：服务名 / 显示名 / 启动类型 / 操作
> 操作：`[改为延迟自动启动]`（仅对「自动」类型显示）、`[恢复为自动]`（仅对「延迟自动」显示）

**② 内核驱动**
> ⚠ 驱动启动顺序由内核即插即用管理器控制，不可延时。此项为只读。
> 列：驱动名 / 显示名 / 启动类型 / 状态

**③ 登录组件（Winlogon / 登录脚本）**
> ⚠ 这些位置一旦改动错误，可能导致**无法登录**。同时它们也是恶意软件常见的藏身处 —— 请核对每一项是否为你认识的程序。
> 列：位置 / 内容 / 风险
> 风险徽标：`系统默认` / `第三方` / `⚠ 可疑`

**④ 已计划的其他自启位置**（可选，🟢C 优先级）
`RunOnce` / `RunServices` / `ShellServiceObjectDelayLoad` 等 —— 仅展示。

---

### 页面 5 · 运行日志

**标题**：运行日志
**副标题**：调度器每次开机的执行记录
**工具栏**：`[日期 ▾ 今天]` `[级别 ▾ 全部]` `[打开日志文件]` `[清空日志]`
**表格**：时间 / 级别 / 条目 / 结果 / 详情
**空状态**：`暂无日志` / `重启电脑后，调度器的执行记录会出现在这里`

---

### 页面 6 · 设置

**顶部固定说明（非设置项）**
> 以下行为是**固定设计**，不提供开关：进入程序时**自动扫描一次**自启动项；关闭窗口**即退出程序**（不常驻托盘，开机调度由计划任务负责）。

**分组：延时启动**
决定弹窗里可选的延时值，以及调度器的存活时长。

| 设置项 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 预设延时选项 | 文本输入（逗号分隔秒数） | `0, 10, 30, 60, 120` | 「延时启动」弹窗里的快捷按钮。用户可改成自己的习惯值（如 `0,15,45,90`） |
| 单条目延时上限 | 下拉 | `24 小时` | 选项：1 小时 / 6 小时 / 24 小时 / 不限制。**仅用于输入校验防呆**，不影响调度能力 |
| **预计调度器驻留时长** | **只读计算值** | 约 1 分 10 秒 | = 最长延时 + 托盘保留 + 启动开销。**自动算出来，不是设置项** |

**分组：调度端**（登录后执行的那个轻量进程，详见 `scheduler-design.md`）

| 设置项 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 显示托盘进度图标 | 开关 | `开` | 关掉后调度端完全无形（"隐形版"），只有完成通知 |
| 完成时通知 | 下拉 | `仅失败时` | 仅失败时 / 总是 / 从不。全部成功是预期结果，默认不打扰 |
| 启动失败时重试 | 下拉 | `1 次` | 不重试 / 1 次 / 2 次 |
| 连续失败后交还自启动 | 下拉 | `3 次` | 关 / 3 次 / 5 次。避免某程序被永久变成"永不启动" |
| 完成后托盘图标保留 | 下拉 | `3 秒` | 3 秒 / 5 秒 / 10 秒。仅图标消失时机，不是面板 |

**分组：启动方式**
- 普通用户程序优先降权启动（CreateProcessAsUser）— `开`
  └ 说明：避免被本程序的管理员权限影响，导致拖拽、剪贴板、文件权限异常
- 降权失败时回退为直接启动 — `开`

**分组：数据**
- 配置目录 — `C:\Users\...\AppData\Local\DelayStart` `[打开目录]`
- `[导出配置]` `[导入配置]` `[重置所有设置]`

**分组：关于**
- 版本 / 构建时间 / 调度器版本 / `[查看许可]`

---

## 四、调度端（登录后执行的轻量进程）界面

> **完整设计已拆分为独立文档：`docs/scheduler-design.md`。**

**原方案已推翻**：早期设计是在屏幕右下角放一个常驻进度面板。**这个设计是错的** —— 调度器的存活时长等于最长延时（用户可设数十分钟），而这恰好是用户刚登录、正在干活的时段。登录后抢屏的窗口，无论做得多精致都是错的。

**新方案（一句话）**：调度端**没有主窗口**，只有一个托盘图标。默认完全隐形；用户悬停看一行摘要、点击弹出面板（失焦即关）；调度结束后发系统通知，**默认只在有失败时发**；点击通知打开管理端「运行日志」页并定位本次运行。

信息通道分 5 级，默认停在 0 级（静默），任何升级都必须由用户主动触发（失败提醒除外）：

| 级别 | 通道 | 用户动作 |
|---|---|---|
| 0 | 完全静默 | — |
| 1 | 托盘图标 + 悬停 tooltip | 主动悬停 |
| 2 | 托盘弹出面板 | 主动点击，失焦即关 |
| 3 | 系统通知（默认仅失败） | 系统弹出，可点击 |
| 4 | 管理端「运行日志」页 | 主动打开完整程序 |

**两条硬技术约束**（详见 `scheduler-design.md` 第五节）：

1. 调度器是管理员权限进程，Windows 通知平台**不支持 elevated 进程发真 Toast**；NativeAOT 也用不了 WinRT / Windows App SDK 的通知 API。只能用托盘气泡通知（`Shell_NotifyIcon`）。
2. Win11 上气泡通知**不进通知中心**（Win10 会留，Win11 变瞬时）。所以通知**不能是唯一通道**，必须有兜底 —— 管理端总览页的「上次运行结果」横幅 + 完整运行日志。

---

## 五、视觉规范

| 项 | 规范 |
|---|---|
| 窗口 | 无系统标题栏，`ExtendsContentIntoTitleBar`，Mica 背景 |
| 主题 | 跟随系统明暗；设置页可强制浅色/深色 |
| 强调色 | 跟随系统 Accent（默认 Win11 蓝） |
| 圆角 | 卡片 8px，控件 4px，窗口 8px |
| 字体 | 标题 `Segoe UI Variable Display Semibold`，正文 `Segoe UI Variable Text`（中文回退 `Microsoft YaHei UI`） |
| 字号 | 页面标题 28 / 卡片标题 14 半粗 / 正文 14 / 辅助文字 12 |
| 图标 | `Segoe Fluent Icons`；列表行使用真实程序图标（`SHGetFileInfo` / `ExtractIconEx`） |
| 间距 | 页面边距 32；卡片内边距 16；列表行高 64（两行式） |
| 状态色 | 已启用 `#8A8A8A` / 已禁用 `#FCE100`（暗）`#9D5D00`（亮） / 已接管 = Accent / 已失效 `#FF99A4` / 受保护 灰+锁 |

---

## 六、决策点与批复结果

> **状态图例**：`✅` 已批复并锁定 · `⏳` 尚未批复（列出建议值，**未批复前不进入编码**）
> **D1–D21 已于 2026-09-19 全部批复完毕，决策冻结，可进入编码。**
>
> - D1–D3、D14–D19：首轮批复
> - D4–D13：2026-09-19 按建议值批量批复
> - D20：**改判为全程提权**（原「按需提权」建议被否，理由见 `requirements.md` NFR-3 注）
> - D21：xUnit v3

| 编号 | 决策 | 选项 | 批复 | 优先级 |
|---|---|---|---|---|
| **D1** | 调度端技术 | A. 独立轻量进程 WinForms+AOT（≈6MB，冷启动 <0.3s）<br>B. WinUI 3 调度端（体积大、冷启动 1–2s，AOT 仍 preview） | **✅ A** | 🔴 |
| **D2** | 主窗口导航 | A. 左侧 NavigationView + 来源子项<br>B. 顶部 TabView（沿用 demo 结构）<br>C. 单页 + 筛选 Chips | **✅ A** | 🔴 |
| **D3** | 界面语言 | A. 仅简体中文<br>B. 中英双语（资源文件） | **✅ A** | 🟡 |
| **D4** | UWP 延时语义 | A. 不做「延时启动 UWP」，UWP 项只能禁用/启用<br>B. 做「禁用系统自启 + 到点 COM 激活」 | **✅ B** —— UI 文案写明"UWP 延时启动会绕过系统的启动管理" | 🟡 |
| **D5** | 「系统启动项」只读页 | A. 做（含服务 delayed-auto 一键切换）<br>B. 只做服务 delayed-auto 切换，其余不做<br>C. 整页砍掉，后置 | **✅ B** —— 服务用系统原生 `delayed-auto`；驱动/Winlogon 不做 | 🟡 |
| **D6** | 程序名 | A. 延时启动管理器 / DelayStart<br>B. 你给一个 | **✅ A** | 🟢 |
| **D7** | 「立即模拟调度」 | A. 做（真的按时间轴启动程序，0.5 倍速预览）<br>B. 只做时间轴动画，不真启动<br>C. 不做 | **✅ A** | 🟢 |
| **D8** | 分发方式 | A. 免安装 zip（unpackaged，自包含）<br>B. MSIX 安装包<br>C. Inno Setup 安装器 | **✅ A → C** —— 第一版免安装 zip，发布阶段补 Inno Setup | 🟢 |
| **D9** | `.lnk` 图标与目标解析 | A. 做（列表更易读）<br>B. 不做，直接显示 .lnk 路径 | **✅ A** | 🟡 |
| **D10** | 批量操作 | A. 做（多选 + 批量禁用/批量延时）<br>B. 不做，第一版先单项 | **✅ B** —— 先跑通单项链路 | 🟢 |
| **D11** | 预设延时选项是否可配置 | A. 只在代码里写死 5 个值<br>B. 设置页可编辑预设值 | **✅ B** —— 逗号分隔文本框驱动弹窗按钮 | 🟡 |
| **D12** | 延时输入上限的语义 | A. 独立的"最大延时上限"限制调度<br>B. 改名「单条目延时上限」，**只做输入校验**，不影响调度 | **✅ B** | 🔴 |
| **D13** | 长延时条目（> 10 分钟）的调度方式 | A. 统一由本程序调度器驻留等待<br>B. 长延时改用 Windows 计划任务 `onlogon + delay`<br>C. 设置里给开关，由用户选 | **✅ A** —— 机制/面板/日志三者统一；驻留代价可忽略 | 🟡 |
| **D14** | 调度端整体形态 | A. **无主窗口**，仅托盘图标 + 点击弹出面板<br>B. 屏幕右下角常驻进度面板（原设计）<br>C. 完全无形，只发通知 | **✅ A** | 🔴 |
| **D15** | 完成后通知策略 | A. 仅失败时通知<br>B. 总是通知<br>C. 从不通知 | **✅ A** | 🔴 |
| **D16** | 面板里是否提供 `[退出调度]` | A. 不提供（急用程序直接双击程序）<br>B. 提供，退出时恢复未启动项的自启动 | **✅ A** | 🟡 |
| **D17** | 启动失败的长期策略 | A. 永久保持接管、静默重试<br>B. 一次失败即交还自启动<br>C. 连续 3 次失败后**自动**交还自启动<br>D. **不自动处理**：保持接管 + 持续升级提醒，最终由用户手动处理 | **✅ D** | 🟡 |
| **D18** | 点击完成通知的行为 | A. 打开管理端「运行日志」页并定位本次运行<br>B. 只弹出托盘面板 | **✅ A** | 🟢 |
| **D19** | 管理端显示"调度进行中"的实时状态 | A. 做（读 `state\current-run.json`）<br>B. 不做，管理端只显示配置 | **✅ A** | 🟢 |
| **D20** | 管理端权限模型 | A. **全程**提权（`app.manifest` → `requireAdministrator`）<br>B. 按需提权（默认普通权限，遇 HKLM 时提示重启为管理员） | **✅ A** —— 原建议 B 被否。**核心功能 100% 需要管理员**，按需提权只省掉"查看"这一步的 UAC，却引入提权重启 + 意图传递 + 半完成状态处理，不划算。详见 6.2 | 🔴 |
| **D21** | 单元测试框架 | A. xUnit v3（`xunit.v3`）<br>B. xUnit v2（SDK 内置模板）<br>C. NUnit / MSTest | **✅ A** —— 主选 xUnit v3；v3 模板安装受阻时降级 B。**NUnit 明确排除**（.NET 10 SDK 下存在 MTP 版本冲突 CS1705）。详见 6.3 | 🟡 |

> **D14–D19 归属调度端**，完整论证、技术硬约束（AOT / elevated 通知限制）、通知文案矩阵、跨进程状态文件设计见独立文档 **`docs/scheduler-design.md`**。
> **D20 / D21 的完整论证见下方 6.2 / 6.3**，工程落地见 `architecture.md` 1.4 与 `build-and-test.md` 第四节。

### 6.1 D17 批复说明：失败由用户处理，软件不代劳

**批复：不自动处理。** 原建议 C（连续 3 次失败后自动交还自启动）被否，理由充分且比原方案更正确：

1. **自动交还自启动 = 软件在用户不知情时改了系统状态**，直接违反本项目"全部可逆、且由用户掌控"的核心原则。
2. **失败原因多数不归本软件管**（程序被卸载、被杀软拦截、路径变更）。自动恢复自启动只是把问题从"不启动"变成"启动失败弹错误框"，还顺手把用户设的延时取消了 —— 问题没解决，配置先丢了。
3. **静默改配置比明示失败更糟**。用户宁可看到一条一直存在的警告，也不愿某天发现"我明明设了延时，怎么没生效"。

**改为三条约束：**

| 约束 | 内容 |
|---|---|
| 保持接管 | 失败项**始终**保持软禁用 + 接管状态，下次登录按原延时继续尝试。软件永不自动修改该项的系统状态 |
| 提醒必须持续 | 失败提示不能只出现一次。托盘角标保留、管理端横幅常驻，直到用户显式处理（见 `scheduler-design.md` 第八节） |
| 出口只有一个 | 管理端横幅上提供 `[移出延时启动]` 直接跳转到该条目的移除确认流程（系统条目走"移除并恢复"），由用户点击完成 —— **软件只给路径，不做决定** |

### 6.2 D20 批复说明：改为全程提权（原「按需提权」建议被否）

**批复：全程 `requireAdministrator`。** 我原来提的"按需提权"是错的。致命的判断错误在于：我按"功能数量占比"评估，而正确的评估口径是**"有没有任何一个功能必须提权"**——只要有一个，按需提权就省不掉那次 UAC，剩下的只是复杂度。

**必须管理员权限的操作：**

| 功能 | 涉及位置 | 不提权的后果 |
|---|---|---|
| **注册调度器计划任务** | Task Scheduler，且需 `RunLevel=Highest` | **软件根本不能工作** |
| 软禁用 HKLM Run 项 | 写 `HKLM\...\StartupApproved\Run` | 拒绝访问 |
| 软禁用 HKLM 32 位 Run 项 | 写 `HKLM\...\StartupApproved\Run32` | 拒绝访问 |
| 软禁用系统启动文件夹项 | 写 `HKLM\...\StartupApproved\StartupFolder` | 拒绝访问 |
| 禁用系统级 / 他人计划任务 | `task.Enabled = false` | 拒绝访问 |
| 服务切 `delayed-auto`（D5） | `ChangeServiceConfig` | 拒绝访问 |

**不需要管理员权限的操作：**

| 功能 | 位置 |
|---|---|
| 扫描 + 软禁用 HKCU Run | `HKCU\...\StartupApproved\Run` |
| 扫描 + 软禁用用户启动文件夹项 | `HKCU\...\StartupApproved\StartupFolder` |
| 只读扫描 HKLM / 计划任务 / 服务 | 默认 ACL 允许读 |
| UWP 自启状态读写 | `HKCU\...\AppModel\SystemAppData` |
| 配置与日志 | `%LOCALAPPDATA%\DelayStart` |

**决定性理由：**

1. **核心链路 100% 阻塞**。不注册调度器，软件连"延时启动"这个主功能都不存在。既然无论如何都要提权，"按需"就只省了"只看不改"这一种用法。
2. **按需提权会制造半完成状态**。用户连点 10 个禁用，HKCU 的 6 个成功、HKLM 的 4 个弹 UAC 或失败——系统进入一个**用户无法理解的中间状态**："为什么这几个禁用了那几个没有？"这比"一开始就不让用"糟得多。
3. **按需提权的实现成本吃掉一个 Phase**：需要 `runas` 重启进程 + 命令行传递待执行意图（哪个页、哪个条目、什么操作）+ 重启后恢复 UI 状态 + 处理用户拒绝 UAC 时的回滚。这些代码全部是纯负担，不产生任何用户价值。
4. **参考同类软件**：Autoruns（微软官方）、Process Explorer、`regedit`、`services.msc`、任务计划程序——**全部是打开即提权**。用户的预期已经建立：系统配置工具就该弹 UAC。
5. **使用频率极低**：配置一次就不再打开。一年弹几次 UAC 完全可接受。
6. **与调度端一致**：调度端本身就是 elevated 进程（D1，为了免 UAC 启动管理员程序）。两端权限一致，语义统一，不需要"我这次启动是什么权限"的心智模型。

**代价与必须配套的三条约束：**

| # | 问题 | 对策 |
|---|---|---|
| 1 | **UAC 被拒后不能降级运行** | 明确提示 `本程序需要管理员权限才能管理自启动项。` + `[重试]` `[退出]`，然后**退出**。半残运行会造成"用户以为禁用成功、实际静默失败"的最坏情况 |
| 2 | **提权窗口接收不到资源管理器的拖放（UIPI）** | 见 `architecture.md` R10。主路径是 `[浏览…]` 按钮（100% 可用）；拖放作为增强，用 `ChangeWindowMessageFilterEx` + 旧式 `WM_DROPFILES` 尝试补上，不通则拖放区降级为纯按钮 |
| 3 | **提权状态下"拖出"到其他应用不工作** | 无解（UIPI 强制）。**不接受"拖 exe 到任务栏固定"这类交互设计** |

**明确不支持：**`以其他用户身份运行`。这会让 `%LOCALAPPDATA%` 指向另一个账户，配置读不到。需在文档与验收清单中写死。

**新增的唯一界面元素：UAC 被拒提示**（这是 D20 改判带来的全部 UI 增量）

```
┌──────────────────────────────────────────────┐
│  ⚠  需要管理员权限                           │
│                                              │
│  延时启动管理器需要管理员权限，才能管理      │
│  HKLM 自启动项、系统启动文件夹与开机调度      │
│  任务。                                       │
│                                              │
│  可以重新启动并同意 UAC 提示，或选择退出。    │
│                                              │
│                    [重试]      [退出]        │
└──────────────────────────────────────────────┘
```

- 在 `App.OnLaunched` **最早期**判断权限，**不构建主窗口**——避免用户看到一个残缺的界面
- `[重试]`：以 `runas` 重新启动当前 exe
- `[退出]`：直接退出
- 文案不出现"部分功能可用"之类的暗示——**不存在半可用状态**（`NFR-3.6`）

### 6.3 D21 批复说明：xUnit v3

**先说清一个事实**：微软对 WinUI 3 测试**没有单一推荐框架**——官方文档（*Test apps built with the Windows App SDK and WinUI 3*）把 MSTest / NUnit / xUnit **三者并列**，内容只讲"引用 WinUI 3 项目时要改 TargetFramework、加 `WindowsAppSDKSelfContained`"。所以选 xUnit 不是"照官方推荐"，而是基于下列技术理由：

| 框架 | 结论 | 理由 |
|---|---|---|
| **xUnit v3** | ✅ **选它** | 单一 NuGet 包（`xunit.v3.mtp-v2`）、原生 MTP、`OutputType=Exe` 可独立运行、`Assert.Skip` 支持条件跳过、无版本冲突。v3 要求 .NET 8+，.NET 10 满足 |
| xUnit v2 | 🟡 降级备选 | SDK 内置模板 `dotnet new xunit` 直接可用（无需额外装模板），但要拖 `xunit.runner.visualstudio` + `Microsoft.NET.Test.Sdk` 走 VSTest 老路径。**若 v3 模板安装受阻就用它**，测试代码本身零改动（`[Fact]`/`[Theory]`/`Assert` 完全一致） |
| NUnit | ❌ 排除 | .NET 10 SDK 下有已知缺陷：`NUnit3TestAdapter` 5.1.0 与 SDK 注入的 `Microsoft.Testing.Platform` 版本冲突，报 **CS1705 且不给出可诊断的包依赖**，必须手动 pin 两个平台包才能编译。**不给项目埋这种雷** |
| MSTest | ❌ 排除 | 其独有价值是 `[UITestMethod]`（XAML 线程测试），而**第一版不做 UI 自动化测试**，用不上；语法也更啰嗦 |

**关键结构性收益**：因为 `DelayStart.App`（WinUI 3 项目）**不被任何测试项目引用**，测试项目就完全不需要走微软那套"WinUI 3 测试项目配置"。TFM 只需跟到 `net10.0-windows`（与 `Core`/`Management` 一致），既不需要 `net10.0-windows10.0.19041.0`，也不需要 `WindowsAppSDKSelfContained`，更不需要预装 Windows App Runtime。测试跑起来是秒级的。

> 这是 1.1 节"按 AOT 兼容性分层"的额外红利：UI 与逻辑彻底分离后，逻辑测试也就不必背 WindowsAppSDK 的包袱。

**主次两条命令路径**（详见 `build-and-test.md` 4.1）：

```bash
# 主路径：先装 v3 模板（一次性）
dotnet new install xunit.v3.templates
dotnet new xunit3 -o tests/DelayStart.Core.Tests -f net10.0
#   随后把 csproj 里 <TargetFramework> 改为 net10.0-windows，与 Core / Management 对齐

# 降级路径：若模板安装失败，用 SDK 内置模板
dotnet new xunit -o tests/DelayStart.Core.Tests -f net10.0
```

---

## 七、本轮评审反馈的处理

### 7.1 自启动项页去掉「手动添加」✅ 已改

「手动添加」只保留在「延时启动」页。自启动项页的职责是「列出系统里已有的东西」，不是创造新东西。

### 7.2 设置页去掉两项配置 ✅ 已改

- 删除「启动时自动扫描自启动项」开关 → 改为**固定行为**：进入程序即扫描一次。
- 删除「关闭窗口时」配置 → 关闭即退出是已确认的设计，没有配置的必要。
- 整个「常规」分组随之删除，改为设置页顶部的一条固定说明。

### 7.3 疑问 1：延时时间怎么选？

**答：预设快选 + 自定义输入，两者都要；预设值本身可在设置里改。**

| 方案 | 判断 |
|---|---|
| 只给定死几个值 | ❌ 不行。延时是强场景相关的 —— 有人要 5 秒，有人要 15 分钟 |
| 只给自由输入框 | ❌ 不行。多一步敲键盘，90% 的情况都是 10/30/60 秒 |
| **预设按钮 + 自定义输入框** | ✅ 主路径一次点击，兜底任意数值 |
| **再加：预设值可在设置里编辑** | ✅ 成本极低，解决个性化 |

所以弹窗长这样：`[登录后立即] [10 秒] [30 秒] [60 秒] [2 分钟]` + `自定义 [__] 秒`，右下角一行小字 `预设值可在「设置」里改`。

**不做**：完整的多选/拖拽式预设编辑器 —— 过度设计，一个逗号分隔的文本框足够。

### 7.4 疑问 2：最大延时上限 —— 你说得对，我的设计有缺陷 ✅ 已改

**原设计的错误**：把「最大延时上限」做成了 10 分钟，这是**用配置去限制调度能力**。用户想设 1 小时延时，软件把它卡在 10 分钟 —— 那就不是"延时启动"，是"启动失败"。这个设置项存在的意义被搞反了。

**修正为两件事，彻底分开：**

| 概念 | 性质 | 归属 |
|---|---|---|
| **单条目延时上限** | 纯输入校验，防呆（防止误填 999999） | 设置项，默认 24 小时，可选"不限制" |
| **调度器驻留时长** | **算出来的**，不是设出来的 | 只读展示 = `max(所有条目的 delaySeconds)` + 面板保留时间 + 启动开销 |

举例（当前配置）：
```
SyncTool         10 秒
微信              30 秒
Epson Scanner    60 秒   ← 最长延时
AutoHotkey       60 秒
─────────────────────────
调度器驻留      60 + 3 + 2 ≈ 1 分 05 秒
```

设置页里这一行是**只读的**，并明确写「调度器会在最后一项启动后自动退出，不会常驻后台」。用户设 1 小时延时，驻留时长就是 1 小时，软件不会、也不该拦着 —— 但要让用户事先知道。

**唯一需要用户配置的"时间"** 是：全部启动完成后进度面板再保留几秒（默认 3 秒）。这才是真正需要人来定的那个值。

### 7.5 搜索框从导航栏移到页面内 ✅ 已改

原设计的错误：导航栏顶部一个全局搜索框，用户在任何页面打字都会被强制跳转到「自启动项」页。**抢走用户的上下文**是明确的 UX 缺陷，不做辩解。

改为两处**页面内搜索**（自启动项页 / 延时启动页），各自独立、只过滤当前页、不跳页、不改来源筛选。完整规则见第二节末「搜索框为什么从导航栏挪进页面」。

放弃全局搜索的理由：本程序总共十几到几十条记录，且导航栏已经按来源分好组、每项带计数。为了"快速定位某程序"而引入一个会抢焦点的全局控件，收益不抵成本。

### 7.6 手动添加 / 编辑弹窗重做 ✅ 已改

原实现只是个 Toast 占位（`原型未展开`），等于没有设计。现在补全，并按「有没有系统来源」分成 3 种形态，共用一套布局：

- **4 个编号步骤**（目标程序 / 延时时间 / 启动身份 / 系统影响或接管对象），把长表单结构化
- **目标程序做成整块拖放区**，选中后原地变「图标 + 名称 + 路径 + [更换]」
- **底部活摘要**：`将在 登录后 30 秒 以 普通用户 身份启动`，随选择实时更新
- **手动条目和系统条目在第 4 块和第 1 块上明确分叉**：手动条目能换文件、写明"不动系统任何东西"；系统条目路径只读、写明"写入软禁用标记，可还原"

完整规范见第三节之二。

同时落定的数据边界：**手动添加的条目不属于系统自启动项，不进入自启动项页**。它只在「延时启动」页出现，不参与"已扫描自启动项"的计数。

---

## 八、评审通过后的实施顺序

**完整的 Phase 划分与出口条件以 `architecture.md` 第十节为准**（Phase 0–6），本节不重复。

本节只记录**与界面相关的技术验证项**——这些是界面能否按本设计落地的前提：

| 时机 | 验证项 | 不通过时的降级 |
|---|---|---|
| **Phase 0** | 🔴 **R9**：`WindowsAppSDKSelfContained=true` 时 `app.manifest` 的 `requireAdministrator` 是否生效 | 按 `architecture.md` R9 三条降级路径（① 去自包含改框架依赖 / ② 运行时 `runas` 自重启 / ③ 外部 AOT 启动器） |
| **Phase 0** | R3：unpackaged 下 `app.manifest` 生效，`PerMonitorV2` DPI 声明生效 | — |
| **Phase 3** | 🟡 **R10**：提权窗口下 `[浏览…]` 必然可用；拖放能否补上 | 拖放区降级为纯按钮，**不留一个"拖进去没反应"的控件** |
| **Phase 3 / 5** | R7：100% / 150% / 200% DPI 下无错位、无文字截断 | — |
| **Phase 4** | R1：NativeAOT 下无边框弹出面板能否工作（`Deactivate` 失焦即关） | 用 `ContextMenuStrip` 承载面板内容（功能等价、视觉弱一些、零风险），见 `scheduler-design.md` 第六节 |

**模拟调度（D7）的验收点**：不等重启就能验证延时配置是否正确。这是开发期的必需品，也是给用户的预览能力，Phase 6 打包前必须可用。

**界面文案验收**：Phase 5 出口要求全部页面文案与本文件逐字对齐。
