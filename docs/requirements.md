# DelayStart — 项目需求规格说明

> **文档边界**：本文是需求的唯一权威来源（What & Why）。
> 界面长什么样见 `design-spec.md`，调度端专项设计见 `scheduler-design.md`，
> 技术实现路径见 `architecture.md`，代码怎么写见 `coding-standards.md`，
> 怎么编译怎么测见 `build-and-test.md`。
>
> 需求编号 `FR-x` / `NFR-x` 是**稳定标识**，代码注释、提交信息、测试用例都应引用。

---

## 一、项目背景与目标

### 1.1 要解决的问题

Windows 的登录自启动机制是**全有或全无**的：一个程序要么在登录瞬间和所有兄弟程序一起抢资源，要么被彻底禁用。现实后果：

- 登录后头 60 秒磁盘 IO 打满、CPU 占满，桌面要等一分多钟才能用
- 用户不想禁掉这些程序（禁了就忘了，或者偶尔要用），但也不想让它们挤在同一个瞬间
- 任务管理器只能"启用/禁用"，不能"晚 30 秒再启动"

### 1.2 产品目标

**扫描所有自启动位置 → 软禁用（不删数据）→ 由独立的调度进程按延时启动。**

三个不可妥协的原则：

| 原则 | 含义 |
|---|---|
| **全部可逆** | 禁用是"软禁用"（写 `StartupApproved` 标记 / `task.Enabled=false` / UWP `State=0`），**绝不删除注册表值、绝不移动用户文件**。任一条目都能一键还原到接管前的状态 |
| **判定可靠** | 延时是"相对登录时刻的绝对时间点"，不是累加。软件必须准确告诉用户"这个程序会在登录后第几秒启动"，而不是"大概" |
| **不替用户做决定** | 启动失败时软件只提醒、不自动改系统状态（D17）。所有会改变系统行为的动作，出口都在用户手上 |

### 1.3 技术目标

| 目标 | 指标 |
|---|---|
| 管理端冷启动 | < 2 秒（WinUI 3 自包含的合理区间） |
| 调度端冷启动 | < 0.3 秒，体积约 6 MB（WinForms + NativeAOT） |
| 全量扫描耗时 | < 2 秒（本机典型 20–40 项） |
| 调度触发偏差 | 相对计划时间点 < ±500 ms |
| 调度端空闲内存 | < 30 MB |

---

## 二、用户与使用场景

**目标用户**：Windows 上装了较多自启动软件的重度用户（开发者、设计师、装机党）。

| 场景 | 用户动作 | 期望结果 |
|---|---|---|
| **首次配置** | 打开管理端 → 看总览 → 逐条把程序加入延时启动 | 5 分钟内把 20 个自启动项排成一条合理的时间轴 |
| **验证配置**（不重启） | 点「立即模拟调度」 | 按真实时间轴（可加速）演示一遍，当场发现"两个程序设同一延时"这类问题 |
| **日常登录** | 无动作 | 登录后各程序按延时依次启动，调度器完成即退出 |
| **查异常** | 点失败通知 / 打开管理端 | 直接看到哪一项失败、原因是什么、连续失败几次 |
| **临时停用某项** | 列表里关掉条目开关 | 该项本次登录不启动，配置保留，原始自启动项状态不变 |
| **彻底还原** | 点「移出延时启动」 | 恢复成接管前的状态，系统按原样启动 |
| **安全体检** | 打开「系统启动项」页 | 看到服务/驱动/Winlogon 等系统级启动项（只读），服务可一键切到"延迟自动启动" |

---

## 三、术语表

| 术语 | 定义 |
|---|---|
| **自启动项**（Startup Entry） | 系统里已存在的、会导致某程序在登录/开机时启动的配置 |
| **软禁用**（Soft Disable） | 不删原始数据，只写"已禁用"标记让系统跳过该项 |
| **接管**（Takeover） | 软禁用原自启动项 + 把该程序纳入本软件的延时启动清单 |
| **延时条目**（Delayed Item） | 被接管、由本软件调度的条目 |
| **管理端**（Manager） | WinUI 3 主程序，负责扫描、配置、查看结果 |
| **调度端**（Scheduler） | 登录后由计划任务拉起的轻量进程，负责按延时启动各程序 |
| **模拟调度** | 在管理端按真实时间轴（加速）跑一遍调度流程，不重启即可验证配置 |
| **稳定主键** | `source + scope + sourceKey` 三元组 + 持久化 `id`，用于可靠识别同一条目 |

---

## 四、产品范围

### 4.1 扫描与接管范围

| 来源 | 位置 | 可禁用 | 可延时启动 | 备注 |
|---|---|---|---|---|
| 注册表 HKCU Run | `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` | ✅ | ✅ | |
| 注册表 HKLM Run | `HKLM\Software\Microsoft\Windows\CurrentVersion\Run` | ✅ | ✅ | 需管理员权限 |
| 注册表 HKLM 32 位 | `HKLM\Software\WOW6432Node\...\Run` | ✅ | ✅ | 标记写入 `StartupApproved\Run32` |
| 用户启动文件夹 | `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup` | ✅ | ✅ | `.lnk` / `.url` |
| 系统启动文件夹 | `%PROGRAMDATA%\Microsoft\Windows\Start Menu\Programs\Startup` | ✅ | ✅ | |
| 计划任务 | 带 `LogonTrigger` / `BootTrigger` 的任务，排除 `\Microsoft\*` | ✅ | ✅ | |
| UWP / Store 应用 | `AppModel\SystemAppData\<PkgFamily>\<TaskId>` | ✅ | ⚠️ 受限 | 见 4.2 |
| 手动添加 | 任意 exe / lnk / msi | — | ✅ | 不进入"自启动项"页 |

### 4.2 UWP 的能力边界（必须写进 UI 文案）

UWP 应用的"自启动"由系统 AppModel 调度，**第三方无法延后它的启动**。可行的语义只有两条：写 `State=0` 禁止、写 `State=2` 允许。

所谓"延时启动 UWP"实际是：**禁用系统自启 + 到点由调度端 COM 激活**。这会绕过系统的启动管理，属于非常规用法，UI 必须明确说明。

> 该项对应决策点 **D4**（✅ 已批复 B：做，文案写明会绕过系统启动管理）。

### 4.3 明确不做的事（红线）

| 不做 | 原因 |
|---|---|
| 删除任何注册表值 / 移动任何用户文件 | 违反"全部可逆"原则。禁用一律走软禁用标记 |
| 接管服务、驱动 | 服务有系统原生的"延迟自动启动"机制（`delayed-auto`），另有正解；驱动启动顺序由内核 PnP 控制，人为延时会设备不可用甚至开机失败 |
| 修改 Winlogon / Userinit / Shell | 改错会导致**无法登录**。只读展示有价值（安全体检），接管没有价值 |
| 修改登录脚本 / GPO 下发的配置 | 会被组策略刷新覆盖，且破坏企业 IT 合规 |
| 启动失败后自动恢复自启动 | D17 已批复：软件不替用户改系统状态 |
| 常驻后台服务 / 开机自启管理端 | 管理端是"用完即走"的工具；常驻的只有登录瞬间的调度端，且跑完即退 |
| 批量操作（第一版） | D10 建议 B：先把单项链路跑通 |

---

## 五、功能性需求

### FR-1 扫描

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-1.1 | 一次扫描覆盖 4.1 表中全部来源 | 各来源的条目都出现在对应子页，计数正确 |
| FR-1.2 | 进入管理端即自动扫描一次（固定行为，不可配置） | 无需手动点刷新就能看到数据 |
| FR-1.3 | 提供手动「刷新扫描」 | 扫描期间有加载态，完成后计数与列表更新 |
| FR-1.4 | 单条读取失败不影响整次扫描 | 某个计划任务读取抛异常时跳过该项并记日志，其余条目正常显示 |
| FR-1.5 | 正确识别软禁用状态 | 键名三级回退匹配（原名 → 原名+`.exe` → 去扩展名），不得把已禁用的项误报为启用 |
| FR-1.6 | 正确识别"已被本软件接管" | 用稳定主键匹配，不因同名条目而误判 |
| FR-1.7 | 解析 `.lnk` 的真实目标与参数、提取真实程序图标 | 启动文件夹中的项显示为真实程序名与图标，而非 `.lnk` 文件名 |
| FR-1.8 | 解析 UWP 显示名 | `ms-resource://` → `SHLoadIndirectString` → MrtCache 反查 → 回退为 PackageFamilyName 前缀 |
| FR-1.9 | 标记受保护 / 只读项 | `\Microsoft\*` 下的计划任务、GPO 下发的任务显示为只读且不可操作 |
| FR-1.10 | 标记已失效项 | 目标文件不存在的条目单独标记（"已失效"），不参与延时配置 |

### FR-2 禁用 / 启用（软禁用）

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-2.1 | 禁用 = 写软禁用标记，原始数据不变 | 禁用后注册表原值、文件均存在；任务管理器里该项显示为"已禁用" |
| FR-2.2 | 启用 = 删除标记值 | 恢复为系统默认状态，原值从未被改动 |
| FR-2.3 | 禁用标记写入正确的位置 | HKCU→`StartupApproved\Run`；HKLM→`StartupApproved\Run`；WOW6432Node→`StartupApproved\Run32`；启动文件夹→`StartupApproved\StartupFolder`（HKCU/HKLM 分别对应） |
| FR-2.4 | 标记数据格式正确 | 12 字节：`[0]=0x03`（禁用）/ `[1..3]=0` / `[4..11]=UTC FILETIME` |
| FR-2.5 | 写标记前确保子键存在 | 用 `CreateSubKey(path, writable: true)`，不因键不存在而失败 |
| FR-2.6 | 访问被拒时给出明确反馈 | 因 ACL / 组策略 / 只读项导致写入被拒时，指出**具体失败项与原因**，不静默失败（管理端全程提权，正常不会遇到"需要提权"场景，见 NFR-3.1） |
| FR-2.7 | 记录接管前的原始状态 | 配置中持久化 `originalState`，用于精确恢复 |

### FR-3 接管（加入延时启动）

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-3.1 | 接管 = 软禁用原项 + 生成延时条目 + 持久化配置 | 三步都成功才算接管成功；任一步失败要回滚并报错 |
| FR-3.2 | 接管前记录原始状态 | `originalState.wasEnabled` 保存接管前的启用状态 |
| FR-3.3 | 自动注册调度用的计划任务 | 首次接管时创建 `DelayStartScheduler` 计划任务；已存在则更新 |
| FR-3.4 | 手动添加的条目不做任何系统改动 | 完全不动注册表/文件，UI 明确告知 |
| FR-3.5 | 支持从 exe / lnk / msi 添加 | 自动填充名称、路径；可选自定义参数 |

### FR-4 延时配置

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-4.1 | 提供预设快选 + 自定义输入两种方式 | 预设按钮一次点击完成；自定义可输入任意秒数 |
| FR-4.2 | 预设值可在设置中编辑 | 默认 `0, 10, 30, 60, 120`，逗号分隔 |
| FR-4.3 | 「单条目延时上限」仅做输入校验 | 默认 24 小时，可选"不限制"。**不影响调度能力**，不是调度上限 |
| FR-4.4 | 启动身份可选：普通用户（推荐）/ 管理员 | 普通用户走降权启动；管理员继承调度端令牌，不弹 UAC |
| FR-4.5 | 可选自定义命令行参数 | 留空则不附加 |
| FR-4.6 | 条目级启用开关 | 关闭后本次登录不启动该项，配置与原始自启动项状态均不变 |
| FR-4.7 | 相同延时内可调整先后顺序 | 提供上移/下移；仅在延时值相同时可用 |
| FR-4.8 | 配置持久化到 `%APPDATA%\DelayStart\config.json`（Roaming，D23） | 采用**原子写**（写 `.tmp` → `File.Replace`）；路径一律经 `PathService` 解析 |

### FR-5 调度执行

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-5.1 | 登录后 3 秒由计划任务拉起，以最高权限运行 | 计划任务 `onlogon` + `delay 0000:03` + `RunLevel=Highest` |
| FR-5.2 | 单实例运行 | 用命名 Mutex 保证，重复触发时立即退出 |
| FR-5.3 | 时序语义 = `remaining = item.DelaySeconds - (now - 本次开始时刻)` | 是**绝对时间点**，非累加；`remaining <= 0` 时立即启动 |
| FR-5.4 | 按 `DelaySeconds` 升序、同值按 `SortOrder` 升序发起 | 输出顺序可被日志验证 |
| FR-5.5 | 逐条 try/catch，单条失败不中断后续 | 日志中有失败原因，后续条目照常启动 |
| FR-5.6 | 「普通用户」条目降权启动 | `WTSQueryUserToken` → `CreateEnvironmentBlock` → `CreateProcessAsUser`，`lpDesktop = winsta0\default` |
| FR-5.7 | 降权失败时回退直接启动并记日志 | 宁可启动成管理员，也不要启动失败 |
| FR-5.8 | 「管理员」条目继承令牌启动，不弹 UAC | 无 UAC 弹窗 |
| FR-5.9 | 启动"成功"的判定：创建成功 + 延时 1.5 秒后复查 `HasExited` | 仍存活 → 成功；已退出且退出码 ≠ 0 → 失败；已退出且退出码 = 0 → 成功（拉起已有实例的正常行为） |
| FR-5.10 | 无任何有效条目时静默退出 | 不弹窗、不显示托盘图标、不发通知 |
| FR-5.11 | 全部条目处理完后自动退出 | 不在后台常驻 |
| FR-5.12 | 写入实时状态供管理端读取 | `%LOCALAPPDATA%\DelayStart\state\current-run.json` 每次状态变化即重写（原子写） |
| FR-5.13 | 结束时归档本次结果 | `runs\<runId>.json`，保留最近 30 次 |
| FR-5.14 | 用源生成 JSON 序列化 | `JsonSerializerContext`，不用反射序列化 |

### FR-6 结果通知与追溯

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-6.1 | 托盘图标显示进度（长任务） | 悬停显示一行摘要（≤63 字符）；预计驻留 < 15 秒则不显示图标 |
| FR-6.2 | 点击托盘图标弹出进度面板 | 无边框、失焦即关、不占任务栏 |
| FR-6.3 | 默认仅在有失败时发通知 | 全部成功不发（设置中可改为"总是" / "从不"） |
| FR-6.4 | 失败通知合并为一条 | 标题 `N 个程序启动失败`，正文列出前 2 项名称 + `点击查看详情` |
| FR-6.5 | 失败项在托盘图标上带红色角标 | 保留至用户点击，或 60 秒后消失 |
| FR-6.6 | 点击通知打开管理端「运行日志」页并定位本次运行 | `DelayStart.exe --goto-log --run=<runId>` |
| FR-6.7 | 管理端总览页显示"上次运行结果"横幅 | 文案矩阵见 `scheduler-design.md` 9.3 |
| FR-6.8 | 失败提醒**持续存在**直到用户处理 | 连续失败 ≥3 次时：托盘角标不自动消失、管理端横幅变为常驻告警条且不提供"忽略" |
| FR-6.9 | 告警条提供 `[移出延时启动]` 作为唯一出口 | 点击后进入该条目的移除确认流程 |
| FR-6.10 | **软件永不自动恢复失败项的自启动** | 任何代码路径都不得自动改写失败项的系统状态 |

### FR-7 系统启动项只读检查

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-7.1 | 只读展示系统服务列表 | 名称 / 显示名 / 启动类型 / 状态 / 描述 |
| FR-7.2 | 服务支持一键切换"延迟自动启动" | 用 Windows 原生机制（`delayed-auto`），**不由本软件调度**；可切回 |
| FR-7.3 | 驱动 / Winlogon / 登录脚本仅只读展示 | 明确标注"系统关键项，本软件不接管" |
| FR-7.4 | 该页所有条目不参与延时配置 | 无"延时启动"按钮 |

> 该项对应决策点 **D5**（✅ 已批复 B：只做服务 `delayed-auto` 切换，驱动 / Winlogon 不做）。

### FR-8 运行日志

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-8.1 | 按 `runId` 分组展示每次登录的调度结果 | 每组显示时间、成功/失败数 |
| FR-8.2 | 每项显示：名称 / 延时 / 结果 / 启动时间 / 失败原因 | 失败项标红，展开可见原因 |
| FR-8.3 | 保留历史运行记录 | 与 `runs\` 目录一致，最多 30 次 |
| FR-8.4 | 支持从通知跳转并定位到指定运行 | 目标运行记录高亮并滚动到可见位置 |
| FR-8.5 | 调度端自身日志 | `%LOCALAPPDATA%\DelayStart\scheduler.log`，滚动截断，管理端可打开所在目录 |

### FR-9 设置

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-9.1 | 页顶固定说明"进入即扫描一次、关闭即退出"，不提供开关 | 无"启动时自动扫描"开关、无"关闭窗口时"配置 |
| FR-9.2 | 预设延时选项可编辑 | 逗号分隔，驱动延时弹窗的按钮 |
| FR-9.3 | 单条目延时上限 | 仅输入校验，默认 24 小时 |
| FR-9.4 | 调度器驻留时长为**只读计算值** | `= max(所有延时) + 面板保留 + 启动开销`，自动算出 |
| FR-9.5 | 全部启动完成后托盘图标保留时长 | 3 / 5 / 10 秒，默认 3 秒 |
| FR-9.6 | 启动失败时重试次数（同一次运行内） | 不重试 / 1 次 / 2 次，默认 1 次 |
| FR-9.7 | 完成时通知策略 | 仅失败时 / 总是 / 从不，默认仅失败时 |
| FR-9.8 | 显示托盘进度图标开关 | 关闭后调度端完全无形 |
| FR-9.9 | 普通用户程序优先降权启动开关 + 降权失败回退开关 | 默认均开启 |
| FR-9.10 | 数据分组：打开配置目录 / 导出配置 / 导入配置 / 重置所有设置 | 重置前必须二次确认并说明"会恢复所有接管项" |
| FR-9.11 | 关于分组：版本 / 构建时间 / 调度器版本 / 查看许可 | |

### FR-10 模拟调度

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-10.1 | 提供「立即模拟调度」，按真实时间轴真启动程序 | 不等重启即可验证延时配置 |
| FR-10.2 | 支持加速播放（默认 4 倍速） | 界面显示当前模拟时钟 |
| FR-10.3 | 可中途停止 | 停止后不残留状态 |
| FR-10.4 | 模拟过程可视化 | 时间轴或进度视图显示各条目的等待/启动中/完成状态 |

> 该项对应决策点 **D7**（✅ 已批复 A：做）。

### FR-11 计划任务注册

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-11.1 | 首次接管时创建 `DelayStartScheduler` 计划任务 | `onlogon` + `delay 3s` + `RunLevel=Highest` |
| FR-11.2 | 程序路径变化时更新任务 | 每次启动管理端校验一次 |
| FR-11.3 | 用 `TaskService` API 创建而非 `schtasks.exe` | 能读取返回、支持中文、参数更细 |
| FR-11.4 | 提供卸载时的清理路径 | 移除全部接管项 + 删除计划任务 |

### FR-12 配置迁移与备份

| 编号 | 需求 | 验收要点 |
|---|---|---|
| FR-12.1 | 支持从 demo 格式（`version=1`）迁移 | 迁移时补齐 `scope` / `enabled` / `originalState` 字段 |
| FR-12.2 | 导入/导出配置 | 导出为单文件 JSON；导入时校验版本并给出差异预览 |
| FR-12.3 | 配置损坏时的恢复 | 解析失败时保留坏文件副本并重建默认配置，提示用户 |

---

## 六、非功能性需求

### NFR-1 性能

| 编号 | 指标 |
|---|---|
| NFR-1.1 | 管理端冷启动 < 2 秒（自包含 WinUI 3） |
| NFR-1.2 | 调度端冷启动 < 0.3 秒，发布体积约 6 MB |
| NFR-1.3 | 全量扫描 < 2 秒；扫描过程不阻塞 UI 线程 |
| NFR-1.4 | 调度触发偏差 < ±500 ms |
| NFR-1.5 | 调度端空闲内存 < 30 MB |
| NFR-1.6 | 列表在 100 条条目下滚动流畅（启用虚拟化） |

### NFR-2 可靠性

| 编号 | 指标 |
|---|---|
| NFR-2.1 | 单条扫描/启动失败不影响其他条目 |
| NFR-2.2 | 所有配置文件原子写入，断电不产生半截文件 |
| NFR-2.3 | 调度端崩溃时留下现场（`current-run.json` 记录进度），管理端可识别"上次未正常完成" |
| NFR-2.4 | 调度端重复触发（多次登录/手动运行）由 Mutex 拦截，不产生并发启动 |
| NFR-2.5 | 配置目录被删除时，管理端能重建并可重新接管 |

### NFR-3 安全与权限

| 编号 | 指标 |
|---|---|
| NFR-3.1 | 管理端**全程以管理员权限运行**（`app.manifest` → `requestedExecutionLevel level="requireAdministrator"`），与调度端权限一致（D20） |
| NFR-3.2 | 绝不删除用户数据（原始注册表值、启动项文件） |
| NFR-3.3 | **被接管的普通用户程序一律走降权启动**（`CreateProcessAsUser`），避免继承管理员令牌导致拖拽/OLE/文件权限异常 |
| NFR-3.4 | 所有会改变系统状态的 UI 操作都有明确的后果说明与二次确认 |
| NFR-3.5 | 不联网、不采集任何用户数据 |
| NFR-3.6 | **UAC 被拒绝时不得降级运行**：给出说明后退出，不进入"部分功能可用"的半残状态 |
| NFR-3.7 | 提权窗口仍需支持从资源管理器拖入文件：主路径为 `[浏览…]` 按钮，拖放为增强项（`ChangeWindowMessageFilterEx` + 旧式 `WM_DROPFILES`） |
| NFR-3.8 | 🔴 **不支持「以其他用户身份运行」与 OTS（over-the-shoulder）提权**：这两种模式下 `%APPDATA%` / `%LOCALAPPDATA%` 指向**另一账户**，配置读不到、日志写错位置。判定方式必须是**比较提权后的用户 SID 与交互会话用户 SID**（取本会话 `explorer.exe` 令牌），**不能用 `IsInRole(Administrator)`** —— 它只回答"是否提权"，回答不了"是不是换了人" |

> **NFR-3.1 注（D20 批复）**：demo 用 `app.manifest requireAdministrator` 全程提权，**新方案沿用这一做法**。
> 我原先提的"按需提权"建议**已被否，理由成立**：核心管理链路（注册调度器计划任务、HKLM 三项软禁用、系统启动文件夹、计划任务、服务 `delayed-auto`）**100% 需要管理员权限**，按需提权省不掉那次 UAC，却要额外承担提权重启、意图传递、UI 状态恢复与拒绝 UAC 时的回滚——成本高、用户零收益，还会制造"部分项处理成功、部分项失败"的半完成状态。
> 同类软件（Autoruns、`regedit`、`services.msc`、任务计划程序）均为打开即提权，用户预期已建立。完整论证见 `design-spec.md` 6.2。

### NFR-4 可维护性

| 编号 | 指标 |
|---|---|
| NFR-4.1 | 分层清晰：Core 层不依赖任何 UI 框架，可被单元测试直接引用 |
| NFR-4.2 | 注册表/文件系统/任务计划操作全部通过接口抽象，可注入假实现 |
| NFR-4.3 | Core 层纯逻辑（路径解析、主键生成、排序、时序计算）有单元测试覆盖 |
| NFR-4.4 | 全部公开 API 有中文 XML 文档注释 |

### NFR-5 兼容性

| 编号 | 指标 |
|---|---|
| NFR-5.1 | Windows 10 21H2 及以上 / Windows 11 全版本 |
| NFR-5.2 | 支持 100% / 125% / 150% / 200% DPI 缩放 |
| NFR-5.3 | 多显示器下调度端面板出现在**主显示器**工作区右下角 |
| NFR-5.4 | 明暗主题均可用（跟随系统，可强制） |
| NFR-5.5 | 系统语言为中文，界面仅简体中文（D3 已批复） |

### NFR-6 分发

| 编号 | 指标 |
|---|---|
| NFR-6.1 | 管理端：WinUI 3 **unpackaged**（`WindowsPackageType=None`）。自包含或框架依赖由 Phase 0 的 R9 实测结果决定 |
| NFR-6.2 | 调度端：WinForms + NativeAOT 单文件 |
| NFR-6.3 | **交付形态：Inno Setup 安装器**。安装到固定路径 `%LOCALAPPDATA%\Programs\DelayStart`（**per-user，不放 Program Files**，D23），**路径不含版本号** → 计划任务 action 长期有效 |
| NFR-6.4 | 🔴 **卸载必须可逆**：卸载时先还原全部被接管条目与软禁用标记，再删文件；还原失败则**中止卸载并说明原因** |
| NFR-6.5 | **安装全程不提权**（`PrivilegesRequired=lowest`，per-user 安装，**零 UAC**）。**卸载会弹一次 UAC** —— 还原 HKLM 接管项与删除计划任务必须有管理员，这一次躲不掉。计划任务**不由安装器注册**，改由**管理端首次启动时幂等注册**（`--reinstall-task`），把提权点收敛到"用户第一次打开程序" |
| NFR-6.6 | 支持 Windows 10 21H2 及以上（与 NFR-5.1 一致）—— 这一条单独就排除了 MSIX 打包方案 |
| NFR-6.7 | 安装目录**只读**：程序不得在安装目录下写任何配置、日志、缓存（保证覆盖升级与卸载干净）。全部运行时数据路径见第七节 |
| NFR-6.8 | 计划任务身份必须是**发起安装的那个交互用户**（`LogonType=Interactive` + `RunLevel=Highest`），🔴 **禁止 SYSTEM / 服务账户** —— 否则 `%LOCALAPPDATA%` 与 `%APPDATA%` 会解析到 SYSTEM 的 profile，配置读不到、日志写到错地方 |

> 该项对应决策点 **D22**（✅ 已批复 A：Inno Setup 安装器 + unpackaged）。原 **D8**（第一版 zip，发布阶段补 Inno Setup）**被 D22 取代**：第一版即出安装器，**不再有 zip 交付形态**。MSIX 被否决的三条理由与完整论证见 `design-spec.md` 6.4。

---

## 七、数据契约

> 路径规范由 **D23** 定义，完整规则与 6 条实现约束见 `architecture.md` 1.5。核心原则：**安装目录与运行时数据完全分离，且配置文件与日志分属 Roaming / Local。**

| 文件 | 路径 | 写入方 | 读取方 | 说明 |
|---|---|---|---|---|
| 程序文件 | `%LOCALAPPDATA%\Programs\DelayStart\` | 安装器 | — | **per-user 安装、只读**；运行时数据一律不落在此（NFR-6.7） |
| 配置 | `%APPDATA%\DelayStart\config.json` | 管理端 | 管理端 + 调度端 | **Roaming**，原子写 |
| 实时状态 | `%LOCALAPPDATA%\DelayStart\state\current-run.json` | 调度端 | 管理端 | 每次状态变化重写 |
| 运行归档 | `%LOCALAPPDATA%\DelayStart\runs\<runId>.json` | 调度端 | 管理端 | 保留最近 30 次 |
| 调度日志 | `%LOCALAPPDATA%\DelayStart\logs\scheduler.log` | 调度端 | 人 | 滚动截断 |
| 管理端日志 | `%LOCALAPPDATA%\DelayStart\logs\manager.log` | 管理端 | 人 | 滚动截断 |
| 备份（弃用） | `%LOCALAPPDATA%\DelayStart\backup\` | — | — | demo 的物理备份目录，新方案主路径走软禁用，**不启用** |

字段定义见 `architecture.md` 第四节。

---

## 八、异常场景矩阵

必须逐条实现对应行为，且每一条都要有测试或手工验证记录。

| # | 场景 | 期望行为 |
|---|---|---|
| E1 | 某计划任务读取抛异常 | 跳过该项 + 记日志，其余照常 |
| E2 | 写 HKLM 标记被拒（ACL / 组策略限制） | 指出**具体条目**与原因，提示该项受系统策略保护，不静默失败 |
| E3 | 目标 exe 已被卸载 | 标记为"已失效"，不参与调度，列表中提示 |
| E4 | 目标 exe 启动后立即退出（退出码 0） | 判定为成功（拉起已有实例的正常行为） |
| E5 | 目标 exe 启动后立即退出（退出码 ≠ 0） | 判定为失败，记录退出码 |
| E6 | 降权启动失败 | 回退直接启动 + 记日志 |
| E7 | 调度端启动时某项延时已过期（`remaining <= 0`） | 立即启动该项 |
| E8 | 调度端被重复拉起 | Mutex 拦截，第二个进程立即退出 |
| E9 | 调度端运行中被强杀 | `current-run.json` 留下现场；管理端识别为"上次调度未正常完成" |
| E10 | `config.json` 损坏 | 保留坏文件副本、重建默认配置、提示用户 |
| E11 | 用户手动改回了注册表（重启某项自启） | 下次扫描显示"已被系统启用"，与接管状态冲突时以**用户手动改的为准**并解除接管 |
| E12 | 计划任务被外部删除 | 管理端检测到并重建，提示用户 |
| E13 | 用户连续 3 次登录某项都失败 | 保持接管不自动处理；托盘角标常驻 + 管理端常驻告警条 + 提供 `[移出延时启动]` |
| E14 | 所有延时项都被关闭（`enabled=false`） | 调度端静默退出，不发通知 |
| E15 | 系统时区/时间被修改 | 时序基于单调计时（`Stopwatch`），不受墙上时钟影响 |
| E16 | GPO 下发的计划任务 | 标记为只读，操作入口禁用 |
| E17 | 用户在 UAC 对话框选择"否" | 说明「本程序需要管理员权限才能管理自启动项」后**退出**，绝不降级运行（NFR-3.6） |
| E18 | 用户以「以其他用户身份运行」或 OTS 提权启动 | **比较提权后的用户 SID 与交互会话用户 SID**，不一致则提示不支持（NFR-3.8）。**不得用 `IsInRole(Administrator)` 代替** —— 它区分不了"正常的 UAC 自提权"与"换了个人" |
| E19 | 程序目录被移动 / 用户 profile 变更（计划任务 action 指向的路径失效） | 启动时对比 `config.json` 记录的安装路径与 `AppContext.BaseDirectory`，不一致则提示并一键 `--reinstall-task`（幂等） |

---

## 九、验收标准

### 9.1 功能验收（必须全通过）

- [ ] 4.1 表中全部 7 类来源均能被扫描到，计数与实际一致
- [ ] 禁用任一来源的条目后，任务管理器 / MSCONFIG 中显示为"已禁用"，且**原始数据完好**
- [ ] 启用后完全还原，无残留标记
- [ ] 接管后重启登录，各程序在**预期时间点**启动（偏差 < 1 秒）
- [ ] 「移出延时启动」后，系统按原样自行启动该程序
- [ ] 模拟调度能真实启动程序并正确反映时间轴
- [ ] 计划任务 `DelayStartScheduler` 创建正确（`onlogon` / `delay 3s` / `Highest`）
- [ ] 失败项：通知发出、角标保留、管理端横幅出现、日志可追溯
- [ ] 连续 3 次失败后：角标不自动消失、告警条不提供"忽略"、**没有任何自动恢复行为**

### 9.2 非功能验收

- [ ] 调度端冷启动 < 0.3 秒（用 `Stopwatch` 在日志中记录）
- [ ] 调度端发布产物约 6 MB
- [ ] 全量扫描 < 2 秒
- [ ] 100% / 150% / 200% DPI 下界面不错位
- [ ] 明暗主题均无对比度问题
- [ ] 调度端空闲时 CPU 占用接近 0，内存 < 30 MB

### 9.3 安全验收

- [ ] 全流程操作后，注册表中 `Run` 下的原值数量与内容**零变化**（用导出对比）
- [ ] 启动文件夹中文件数量与内容**零变化**
- [ ] 未接管的计划任务 `Enabled` 状态零变化
- [ ] 卸载/重置后系统回到接管前状态

### 9.4 分发验收（D22 / D23）

- [ ] 安装到**固定路径** `%LOCALAPPDATA%\Programs\DelayStart`，路径中**不含版本号**
- [ ] **安装过程无 UAC 提示**（per-user 安装，`PrivilegesRequired=lowest`）；**卸载弹一次 UAC**（还原接管项 + 删计划任务必需，属预期行为）
- [ ] 首次启动管理端时弹**一次** UAC（程序 manifest 提权）并幂等注册 `DelayStartScheduler`，action 指向安装目录、身份为**当前交互用户**（🔴 不是 SYSTEM）
- [ ] 程序运行时安装目录内**零新增文件**（只读约束）
- [ ] 安装后**不依赖用户手动预装任何运行时**（自包含，或安装器已代为部署 Windows App Runtime）
- [ ] 🔴 **卸载可逆性**：接管若干条目后执行卸载 → 重启 → **被接管的程序全部恢复自启动**，注册表 `Run` 与操作前导出**完全一致**
- [ ] 🔴 卸载时故意让还原失败（如占用/权限）→ **卸载中止并给出原因**，文件未被删除，系统状态未被半还原
- [ ] 卸载后 `%APPDATA%\DelayStart` 与 `%LOCALAPPDATA%\DelayStart` **仍存在**（默认保留配置与日志）；勾选"同时删除个人数据"后才消失
- [ ] 覆盖升级（装新版本到同一目录）后，计划任务仍有效、`%APPDATA%\DelayStart\config.json` 未被清空

---

## 十、需求追踪矩阵

| 需求域 | 设计文档 | 实现层 | 测试方式 |
|---|---|---|---|
| FR-1 扫描 | `design-spec.md` 页面 2 / 4 | `Core/StartupSources/*` | 单元测试（解析逻辑）+ 手工（真机扫描） |
| FR-2 软禁用 | `api-analysis.md` 1.2 / 二 | `Core/StartupSources/*` | 手工（注册表导出对比）+ 单元测试（标记数据格式） |
| FR-3 接管 | `design-spec.md` 三之二 | `Core/Services/TakeoverService` | 手工端到端 |
| FR-4 延时配置 | `design-spec.md` 三之二 / 页面 3 | `Core/Services/ConfigService` | 单元测试（校验/排序/持久化） |
| FR-5 调度执行 | `scheduler-design.md` 第七 / 八节 | `Scheduler/*` | 手工（真机重启）+ 日志校验 |
| FR-6 通知追溯 | `scheduler-design.md` 第三 / 五 / 九节 | `Scheduler/*` + `App/Views/LogView` | 手工 |
| FR-7 系统启动项 | `design-spec.md` 页面 4 | `Core/Services/ServiceQuery` | 手工 |
| FR-8 运行日志 | `design-spec.md` 页面 5 | `App/Views/LogView` | 手工 |
| FR-9 设置 | `design-spec.md` 页面 6 | `App/Views/SettingsView` | 手工 |
| FR-10 模拟调度 | `design-spec.md` 页面 3 | `Core/Services/SimulationService` | 手工 |
| FR-11 计划任务注册 | `api-analysis.md` 3.1 | `Core/Services/TaskRegistrationService` | 手工 |
| FR-12 配置迁移 | `architecture.md` 第四节 | `Core/Services/ConfigService` | 单元测试 |
| NFR-1 性能 | — | 全部 | 手工实测 |
| NFR-2 可靠性 | — | 全部 | 单元测试 + 手工 |
| NFR-3 安全 | `api-analysis.md` 二 | 全部 | 手工（注册表导出对比） |
| NFR-4 可维护性 | `architecture.md` | 全部 | 代码评审 |

---

## 十一、未决项（不批复不编码）

| 编号 | 内容 | 批复 | 状态 |
|---|---|---|---|
| **D4** | UWP 是否支持"延时启动" | B：做，文案写明会绕过系统启动管理 | ✅ |
| **D5** | 「系统启动项」页做到什么程度 | B：只做服务 `delayed-auto` 切换 | ✅ |
| **D6** | 程序名 | A：延时启动管理器 / DelayStart | ✅ |
| **D7** | 「立即模拟调度」是否做 | A：做 | ✅ |
| **D8** | 分发方式 | ~~A → C：第一版 zip，发布阶段补 Inno Setup~~ → **被 D22 取代：直接走 C** | ✅ |
| **D9** | `.lnk` 目标与图标解析 | A：做 | ✅ |
| **D10** | 批量操作 | B：第一版不做 | ✅ |
| **D11** | 预设延时值可配置 | B：设置页可编辑 | ✅ |
| **D12** | 延时上限语义 | B：仅输入校验 | ✅ |
| **D13** | 长延时条目的调度方式 | A：统一由本程序调度器驻留 | ✅ |
| **D20** | 管理端权限模型 | **A：全程提权**（原「按需提权」建议被否） | ✅ |
| **D21** | 单元测试框架 | **A：xUnit v3**，主选 v3 模板 / 降级 SDK 内置模板；**NUnit 明确排除** | ✅ |
| **D22** | 分发形态（MSIX 是否可行） | **A：Inno Setup 安装器 + unpackaged**。MSIX 被否决（Win10 无法提权 / 注册表虚拟化 / 路径含版本号） | ✅ |
| **D23** | 安装与数据布局 | **per-user 不提权安装**：程序 `%LOCALAPPDATA%\Programs\DelayStart`（只读）；配置 `%APPDATA%\DelayStart\config.json`（Roaming）；日志/状态/归档 `%LOCALAPPDATA%\DelayStart\`（Local） | ✅ |
| **D24** | 调度端 UI 技术选型（**重开 D1**） | A. 维持 WinForms+AOT + 内部属性 `_SuppressWinFormsTrimError`（改动最小但官方不支持）<br>**B. 改纯 Win32**（`Shell_NotifyIcon` + 自绘弹窗，彻底离开灰色地带）<br>C. 改 WinUI 3+AOT（官方支持但冷启动 0.5–0.7s 超 NFR-1.2，且 WinUI 3 无托盘 API）<br>D. 放弃 AOT 改框架依赖（体积/冷启动不可接受） | **✅ B（2026-09-19 批复）** —— 调度端 UI 归零重写为纯 Win32，不再引用任何 UI 框架。Phase 0 已按 B 落地 | 🔴 |

| **D25** | Phase 0 模板残留页（`Pages/` 的 Home / About / Settings）怎么处理 | A. **现在就删**，导航壳留空、Phase 3 按文档从零建 6 个页面<br>B. 留到 Phase 3 整体替换 | **✅ A（2026-09-19 批复）** —— 模板页与 `architecture.md` 1.4 的页集不符（`SettingsPage` 名字撞了但内容是占位符），且**不预置死链导航项**。已执行：删 `Pages/` 全 6 文件 + 清空 `MainWindow` 导航项 | 🟢 |
| **D26** | 测试命令：`dotnet test` 集成有缺陷，怎么走 | A. **只改命令**：规范用 `dotnet run --project tests/DelayStart.Core.Tests -c Release`，不动包<br>B. 补 `xunit.runner.visualstudio` + 从 `global.json` 去掉 MTP 声明，退回 VSTest 路径（VS 测试资源管理器也能用）<br>C. 换掉 `xunit.v3.mtp-v2`，改用不带 `-mtp-v2` 变体的 `xunit.v3` | ⏳ **待决策** —— AI 建议 **A 先落地（已生效）**，B/C 属优化。**不阻塞 Phase 1**：测试照写，只是命令不同 | 🟡 |

> **D1–D25 已全部批复完毕，决策冻结，可进入编码。** 完整选项与论证见 `design-spec.md` 第六节（6.1 D17 / 6.2 D20 / 6.3 D21 / 6.4 D22 / 6.5 D23 / **6.6 D24**）。D23 的 6 条实现规则见 `architecture.md` 1.5。
>
> **D26 背景（Phase 0 实测）**：`xunit.v3.mtp-v2` 4.0.1 + `Microsoft.Testing.Platform` 2.4.0 + .NET SDK 10.0.401 组合下，
> `dotnet test` 报"运行了零个测试"并返回退出码 5，**但测试发现本身是好的**（xUnit 自带运行器能枚举到全部用例）。
> 逐一排除了 `IsTestProject`、参数转发、包版本冲突三种可能。详见 `build-and-test.md` 4.2。
>
> **D24 为什么重开 D1**：Phase 0 实测发现 `UseWindowsForms=true` + `PublishAot=true` 会被 SDK 主动拦截（`NETSDK1175`），官方明确把 WinForms 列为 trimming/AOT 不支持，放行只能靠**内部属性**。D1 当初是在不知道这件事的前提下做的决策。详见 `architecture.md` R11。
