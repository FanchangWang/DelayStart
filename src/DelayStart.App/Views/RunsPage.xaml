<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DelayStart.App.Views.RunsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DelayStart.App.ViewModels"
    mc:Ignorable="d">

    <!-- 「调度日志」页（FR-8）：按次分组的调度结果，最新在前，保留最近 30 次 -->
    <Page.Resources>
        <!-- 🔴 行容器的左右内缩固定为 0（2026-09-23 批复 26）。
             左右内缩**只由两处决定**：卡片 Border + 行 Grid 的 Padding。
             列头不在 ListViewItem 里面（它在 Expander 的内容 StackPanel 里），
             所以它的 Padding 只能等于这两者之和（20 = 卡片 8 + 行 12）。 -->
        <Style x:Key="RunRowStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinHeight" Value="0" />
        </Style>
    </Page.Resources>

    <Grid Padding="32,20,32,24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 页头 -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="调度日志" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind ViewModel.Subtitle, Mode=OneWay}" />
        </StackPanel>

        <!-- 工具栏（D4 A）：级别筛选 + 打开日志目录 -->
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ComboBox
                Grid.Column="0"
                MinWidth="130"
                VerticalAlignment="Center"
                SelectedIndex="{x:Bind ViewModel.LevelFilterIndex, Mode=TwoWay}">
                <ComboBoxItem Content="全部级别" />
                <ComboBoxItem Content="仅含失败" />
            </ComboBox>

            <Button
                Grid.Column="2"
                Command="{x:Bind ViewModel.OpenLogsCommand}"
                Content="打开日志文件" />
        </Grid>

        <ProgressBar Grid.Row="2" IsIndeterminate="True" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />

        <!-- 分组列表：一次登录 = 一组（D2 批复 A：列表包进卡片） -->
        <Border
            Grid.Row="3"
            Padding="8,4"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="7">
            <!-- 外层分组容器也归零（2026-09-24 用户批复）：不挂样式会落到 WinUI 默认
                 ListViewItem（Padding 12,0、MinHeight 40），整组（组标题 + 表头 + 行）
                 都比卡片边框多缩 12。列头与行**都在这层之内**，所以归零不改变两者的
                 相对基线，只是让分组标题与其它列表页的行内容对齐同一条内缩。 -->
            <ListView
                ItemContainerStyle="{StaticResource RunRowStyle}"
                ItemsSource="{x:Bind ViewModel.Groups}"
                SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:RunGroupRow">
                    <!-- IsExpanded：进入页面时最新一组默认展开（2026-09-21 批复）；
                         分组随筛选重填会重新生成容器，OneTime 求值即可。 -->
                    <Expander
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        IsExpanded="{x:Bind IsExpanded, Mode=OneTime}">
                        <Expander.Header>
                            <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}" />
                        </Expander.Header>
                        <StackPanel>
                            <!-- 表头：左右内缩 = 行 Grid 的 12。
                                 🔴 列头在 Expander 内容的 StackPanel 里、**不在**内层
                                 ListViewItem 里，与行之间只剩一个内缩已归零的容器；
                                 卡片 Border 的 8 是两边共同吃掉的 —— 此前写成 28
                                 （卡片 8 + 容器 8 + 行 12）导致列头比行多缩 8（批复 26）。
                                 列标题**一律左对齐**，与行内文字共用同一条左基线
                                 （2026-09-23 批复 25）。TextBlock 默认就是
                                 HorizontalAlignment=Stretch + TextAlignment=Left，
                                 所以这里不写对齐属性即为左对齐。 -->
                            <Grid Padding="12,8,12,2" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="程序" />
                                <TextBlock
                                    Grid.Column="1"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="延时" />
                                <TextBlock
                                    Grid.Column="2"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="结果" />
                                <TextBlock
                                    Grid.Column="3"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="说明" />
                            </Grid>

                            <ListView
                                ItemContainerStyle="{StaticResource RunRowStyle}"
                                ItemsSource="{x:Bind Items}"
                                SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="vm:RunItemRow">
                                        <Grid Padding="12,5" ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="70" />
                                                <ColumnDefinition Width="90" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Text="{x:Bind Name}"
                                                TextTrimming="CharacterEllipsis" />

                                            <TextBlock
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Text="{x:Bind DelayText}" />

                                            <TextBlock
                                                Grid.Column="2"
                                                VerticalAlignment="Center"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Text="{x:Bind StateText}" />

                                            <TextBlock
                                                Grid.Column="3"
                                                VerticalAlignment="Center"
                                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Text="{x:Bind ReasonText}"
                                                TextTrimming="CharacterEllipsis"
                                                ToolTipService.ToolTip="{x:Bind ReasonText}" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </Expander>
                </DataTemplate>
            </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- 空状态 -->
        <StackPanel
            Grid.Row="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="6"
            Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay}">
            <TextBlock
                HorizontalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextAlignment="Center"
                Text="还没有调度记录" />
            <TextBlock
                HorizontalAlignment="Center"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                TextAlignment="Center"
                Text="接管条目并重启后，每次登录的启动结果都会出现在这里" />
        </StackPanel>
    </Grid>
</Page>
